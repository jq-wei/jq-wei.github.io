<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jq-wei.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jq-wei.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-09T15:45:47+00:00</updated><id>https://jq-wei.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Action, Planning, and Memory: The Foundation of LLM Agents</title><link href="https://jq-wei.github.io/blog/2026/action-planning-memory-foundation-llm-agents/" rel="alternate" type="text/html" title="Action, Planning, and Memory: The Foundation of LLM Agents"/><published>2026-03-09T00:00:00+00:00</published><updated>2026-03-09T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2026/action-planning-memory-foundation-llm-agents</id><content type="html" xml:base="https://jq-wei.github.io/blog/2026/action-planning-memory-foundation-llm-agents/"><![CDATA[<p>Large language models (LLMs) have demonstrated remarkable capabilities across a wide range of tasks, yet their underlying mechanism remains surprisingly simple: they generate text token-by-token in a sequence. However, at least for the basic transformer inference, this generation is passive. To transform these models from passive text generators into capable autonomous agents, we must look beyond standard prompting paradigms. This post explores one foundational framework (main contribution by Shunyu Yao) that provide the missing architectural pillars for true agency:</p> <p><strong>ReAct</strong> (equipping models with external actions and observations), <strong>Tree of Thoughts</strong> (upgrading internal reasoning to deliberate, planning and search), and <strong>Reflexion</strong> (providing dynamic memory to learn from past successes and failures). Together, action, planning, and memory form the fundamental blueprint for the next generation of AI agents.</p> <h1 id="react">ReAct</h1> <p>This is the most cited work by Shunyu Yao so far.</p> <p>This work defines a paradigm of interacting with LLM, especially with explosion of LLM agent. In fact, more recent LLMs have reasoning capability as builtin capability, which essentially means ReAct is enabled by default. ReAct enjoys several unique features:</p> <ol> <li>Intuitive and easy to design: it is close to human way of solving problems. In the prompt, just need the human annotator write down their thoughts during problem-solving.</li> <li>Less hallucination comparing to Chain-of-thought.</li> </ol> <h2 id="core-idea-of-react">Core idea of ReAct</h2> <p>The basic interaction of an agent with an environment for a task is composed by</p> <ul> <li>at time $t$, agent receives an observation $o_t\in \mathcal{O}$ from the env</li> <li>takes an action $a_t\in\mathcal{A}$ following some policy $\pi(a_t|c_t)$</li> <li>$c_t = (o_1, a_1, \ldots, o_{t-1}, a_{t-1}, o_t)$ is the context to the agent.</li> </ul> <p>The authors proposed a very reasonable and intuitive expansion: augment the action space as $\hat{\mathcal{A}} = \mathcal{A}\cup \mathcal{L}$, where $\mathcal{L}$ is the space of language (for thought and reasoning). Now, besides the action and observation, the full interaction between an agent and an env is expanded with ‘thought’ which is</p> <ul> <li>free-form language thoughts</li> <li>decompose goals,</li> <li>track progress</li> <li>handle exceptions</li> </ul> <h2 id="prompt-design">Prompt design</h2> <p>This paper proposed some prompt templates for different use case. Note all these cases is using few-shot prompt, namely no special prefix design but including some examples in the prompt.</p> <h3 id="hotpotqa">HotpotQA</h3> <p><strong>Dense thoughts</strong>: this eval set is more knowledge intensive, hence a thought accompanies every action to guide the search and synthesis of the info.</p> <p>One example is attached as following:</p> <p><img src="/assets/img/llm-agents/Screenshot_2025-12-26_at_10.07.08.png" width="80%" alt="HotpotQA Example 1"/></p> <p><img src="/assets/img/llm-agents/Screenshot_2025-12-26_at_10.07.42.png" width="80%" alt="HotpotQA Example 2"/></p> <p><img src="/assets/img/llm-agents/Screenshot_2025-12-26_at_10.07.58.png" width="80%" alt="HotpotQA Example 3"/></p> <p>This type is for dense thought, for sparse thought, the model dose not need to think at every step, but asynchronously as needed.</p> <h2 id="why-react-can-be-learned-by-llm">Why ReAct can be learned by LLM?</h2> <p>First, this paper discussed fine-tune a LLM to boost the ReAct performance. The author used a base model PaLM-8/62B as a candidate. Without finetuning, ReAct perform the worst. But with only 3000-example fine-tuning, the ReAct outperform all the others.</p> <p><img src="/assets/img/llm-agents/Screenshot_2025-12-30_at_10.48.54.png" width="70%" alt="ReAct Finetuning Results"/></p> <p>The authors also compared the ReAct Performance of PaLM-540B and GPT3. GPT3 is better means the model there can follow the ReAct prompt better than PaLM-540B. This is probably because the instruct fine tuning done towards GPT3 with some human thinking and action steps in the samples.</p> <p><img src="/assets/img/llm-agents/Screenshot_2025-12-30_at_13.02.41.png" width="70%" alt="Model Comparison"/></p> <h2 id="in-context-learning-and-zero-shot-react-agent">In-context-learning and zero-shot ReAct agent</h2> <p>In this paper, the authors are using standard in-context-learning with few-shot examples in the prompt for the LLM to learn ReAct. Since then it has become a foundational paradigm for LLM agents. Now in langchain, it provide a zero-shot ReAct agent (Langchain incorporate agent endpoint to <code class="language-plaintext highlighter-rouge">create_agent</code> now, and zero-shot ReAct agent <code class="language-plaintext highlighter-rouge">create_react_agent</code> is moved to <code class="language-plaintext highlighter-rouge">langchain_classic</code>). In this section, we explore some behaviors of this type of agent.</p> <p>The skeleton of the code is as follows, where we defined a zero-shot ReAct agent with 3 basic tools.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain_core.tools</span> <span class="kn">import</span> <span class="n">tool</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">PromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_classic.agents</span> <span class="kn">import</span> <span class="n">create_react_agent</span><span class="p">,</span> <span class="n">AgentExecutor</span>

<span class="c1"># Load environment variables from .env
</span><span class="nf">load_dotenv</span><span class="p">()</span>

<span class="c1"># ============================================================================
# Define Tools
# ============================================================================
</span>
<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">search_wikipedia</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Search Wikipedia for information about people, places, events, concepts.</span><span class="sh">"""</span>
    <span class="kn">import</span> <span class="n">wikipediaapi</span>
    <span class="n">wiki</span> <span class="o">=</span> <span class="n">wikipediaapi</span><span class="p">.</span><span class="nc">Wikipedia</span><span class="p">(</span><span class="n">user_agent</span><span class="o">=</span><span class="sh">'</span><span class="s">ReActTestAgent/1.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">language</span><span class="o">=</span><span class="sh">'</span><span class="s">en</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">page</span> <span class="o">=</span> <span class="n">wiki</span><span class="p">.</span><span class="nf">page</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">page</span><span class="p">.</span><span class="nf">exists</span><span class="p">():</span>
        <span class="k">return</span> <span class="n">page</span><span class="p">.</span><span class="n">summary</span><span class="p">[:</span><span class="mi">500</span><span class="p">]</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">No Wikipedia page found for </span><span class="sh">'</span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="sh">'</span><span class="s">.</span><span class="sh">"</span>

<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">calculate</span><span class="p">(</span><span class="n">expression</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Evaluate a mathematical expression. Examples: </span><span class="sh">'</span><span class="s">2 + 2</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">10 * 5</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">(100 - 20) / 4</span><span class="sh">'</span><span class="s">.</span><span class="sh">"""</span>
    <span class="n">allowed_chars</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="sh">"</span><span class="s">0123456789+-*/(). </span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="nf">all</span><span class="p">(</span><span class="n">c</span> <span class="ow">in</span> <span class="n">allowed_chars</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">expression</span><span class="p">):</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">Error: Only basic math operations allowed</span><span class="sh">"</span>
    <span class="k">return</span> <span class="nf">str</span><span class="p">(</span><span class="nf">eval</span><span class="p">(</span><span class="n">expression</span><span class="p">))</span>

<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">get_current_year</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Get the current year.</span><span class="sh">"""</span>
    <span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>
    <span class="k">return</span> <span class="nf">str</span><span class="p">(</span><span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">().</span><span class="n">year</span><span class="p">)</span>

<span class="c1"># ============================================================================
# Create Agent
# ============================================================================
</span>
<span class="k">def</span> <span class="nf">create_agent</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">Create a ReAct agent with explicit reasoning traces.</span><span class="sh">"""</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">search_wikipedia</span><span class="p">,</span> <span class="n">calculate</span><span class="p">,</span> <span class="n">get_current_year</span><span class="p">]</span>
    <span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    
    <span class="c1"># ReAct prompt template from the paper
</span>    <span class="n">template</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Answer the following questions as best you can. You have access to the following tools:

{tools}

Use the following format:

Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take, should be one of [{tool_names}]
Action Input: the input to the action
Observation: the result of the action
</span><span class="gp">...</span> <span class="p">(</span><span class="n">this</span> <span class="n">Thought</span><span class="o">/</span><span class="n">Action</span><span class="o">/</span><span class="n">Action</span> <span class="n">Input</span><span class="o">/</span><span class="n">Observation</span> <span class="n">can</span> <span class="n">repeat</span> <span class="n">N</span> <span class="n">times</span><span class="p">)</span>
<span class="n">Thought</span><span class="p">:</span> <span class="n">I</span> <span class="n">now</span> <span class="n">know</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span>
<span class="n">Final</span> <span class="n">Answer</span><span class="p">:</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span> <span class="n">to</span> <span class="n">the</span> <span class="n">original</span> <span class="nb">input</span> <span class="n">question</span>

<span class="s">Begin!

Question: {input}
Thought:{agent_scratchpad}</span><span class="sh">"""</span>
    
    <span class="n">prompt</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="n">template</span><span class="p">)</span>
    <span class="n">agent</span> <span class="o">=</span> <span class="nf">create_react_agent</span><span class="p">(</span><span class="n">llm</span><span class="p">,</span> <span class="n">tools</span><span class="p">,</span> <span class="n">prompt</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="nc">AgentExecutor</span><span class="p">(</span>
        <span class="n">agent</span><span class="o">=</span><span class="n">agent</span><span class="p">,</span>
        <span class="n">tools</span><span class="o">=</span><span class="n">tools</span><span class="p">,</span>
        <span class="n">verbose</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">handle_parsing_errors</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">max_iterations</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
        <span class="n">return_intermediate_steps</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>

<span class="c1"># ============================================================================
# Main
# ============================================================================
</span>
<span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="n">agent</span> <span class="o">=</span> <span class="nf">create_agent</span><span class="p">()</span>
    
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">question</span> <span class="o">=</span> <span class="nf">input</span><span class="p">(</span><span class="sh">"</span><span class="s">Question: </span><span class="sh">"</span><span class="p">).</span><span class="nf">strip</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">question</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">()</span>
            <span class="n">result</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span> <span class="n">question</span><span class="p">})</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">Final Answer: </span><span class="si">{</span><span class="n">result</span><span class="p">[</span><span class="sh">'</span><span class="s">output</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="nf">main</span><span class="p">()</span>
</code></pre></div></div> <p>If we ask the agent “If Wolfgang Amadeus Mozart had lived as long as Ludwig van Beethoven did, what year would Mozart have died?”, we got the following chain:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reasoning: First, I need to find out when Mozart was born and when he died to calculate his age. Then, I need to find out when Beethoven was born and when he died to calculate his age. If Mozart had lived as long as Beethoven, I can add Beethoven's age to Mozart's birth year to find out the year Mozart would have died.

Action: search_wikipedia
Action Input: "Wolfgang Amadeus Mozart"
Observation: Wolfgang Amadeus Mozart (27 January 1756 – 5 December 1791) was a prolific and influential composer of the Classical period. Despite his short life, his rapid pace of composition and proficiency from an early age resulted in more than 800 works representing virtually every Western classical genre of his time...

Reasoning: Now I know that Mozart was born in 1756 and died in 1791. I need to calculate his age.
Action: calculate
Action Input: "1791 - 1756"
Observation: 35

Reasoning: Mozart was 35 years old when he died. Now I need to find out when Beethoven was born and when he died to calculate his age.
Action: search_wikipedia
Action Input: "Ludwig van Beethoven"
Observation: Ludwig van Beethoven (baptised 17 December 1770 – 26 March 1827) was a German composer and pianist...

Reasoning: Now I know that Beethoven was born in 1770 and died in 1827. I need to calculate his age.
Action: calculate
Action Input: "1827 - 1770"
Observation: 57

Reasoning: Beethoven was 57 years old when he died. Now, if Mozart had lived as long as Beethoven, I can add Beethoven's age to Mozart's birth year to find out the year Mozart would have died.
Action: calculate
Action Input: "1756 + 57"
Observation: 1813

Reasoning: I now know the final answer
Final Answer: If Wolfgang Amadeus Mozart had lived as long as Ludwig van Beethoven did, Mozart would have died in the year 1813.
</code></pre></div></div> <h1 id="tot">ToT</h1> <p>Tree of Thoughts is a framework that empowers LMs to more autonomously and intelligently make decisions and solve problems. It is motivated by cognitive research back to 1950s.</p> <p><em>A genuine problem-solving process involves the repeated use of available information to initiate exploration, which discloses, in turn, more information until a way to attain the solution is finally discovered.—— Newell et al.</em></p> <p>In this paper, ToT is limited to pure reasoning and search problems that do not require external interaction. But it fits naturally with ReAct agent paradigm. It can be seen as the guide for the thinking process of the agent, and the state can contain the observation after apply the action proposed by the thought.</p> <h2 id="notation">Notation</h2> <ul> <li>$p_\theta$: pretrained LM with parameter $\theta$</li> <li>lowercase letter to denote a language seq: $x = (x[1], \cdots, x[n])$ where $x[i]$ is a token</li> <li>uppercase letter to denote a collection of seq: $S$</li> <li>Input-output prompting: $y \sim p_\theta^{IO}(y | x)$ where x is the total input to the LM.</li> </ul> <h2 id="chain-of-thought">Chain of thought</h2> <p>Given the notation above, we can denote the CoT, which is a one-shot LLM call, as</p> \[[z_{1...n}, y] \sim p_\theta^{CoT}(z_{1...n}, y | x)\] <p>where $z_i$ is the each thought and y is the final output. In other words, in CoT, the LLM samples the entire sequence of intermediate thoughts and the final output as one <strong>“continuous language sequence”</strong> based on the initial prompt.</p> <h2 id="tree-of-thought">Tree of thought</h2> <h3 id="general-framework">General framework</h3> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-08_at_07.31.43.png" width="70%" alt="Tree of Thoughts Framework"/></p> <p>Sequence generations like CoT suffer from these two key shortcomings:</p> <ul> <li><em>Locally, they do not explore different continuations within a thought process – the branches of the tree.</em></li> <li><em>Globally, they do not incorporate any type of planning, lookahead, or backtracking to help evaluate these different options – the kind of heuristic-guided search that seems characteristic of human problem-solving.</em></li> </ul> <p>ToT frames any problem solving process as a search over a tree, where each node in the tree is a state $s = [x, z_{1\ldots i}]$ representing a partial solution with the input and the sequence of thoughts so far.</p> <p>ToT address these shortcomings from four perspectives:</p> <ol> <li><em>How to decompose the intermediate process into thought steps;</em></li> <li><em>How to generate potential thoughts from each state;</em></li> <li><em>How to heuristically evaluate states;</em></li> <li><em>What search algorithm to use.</em></li> </ol> <p><strong>1 Thought decomposition</strong></p> <p>This part is task dependent, but the general guide is: a thought should be “small” enough so that LMs can generate promising and diverse samples, yet “big” enough so that LMs can evaluate its prospect toward problem solving.</p> <p><strong>2 Thought generator</strong></p> <p>Given state $s = [x, z_{1\ldots i}]$, the authors promote two strategies to generate k candidates for the next thought:</p> <ul> <li>Sample: $z^{(j)} \sim p_\theta^{CoT}(z_{i+1} | s) = p_\theta^{CoT}(z_{i+1} | x, z_{1…i}), j = 1 \cdots k$. This is suitable if the thought space is rich (continuous), for example creative writing.</li> <li>Propose: $[z^{(1)}, \cdots, z^{(k)}] \sim p_\theta^{propose}(z_{i+1}^{(1 \cdots k)} \mid s).$ This is more suitable for tasks with a more constrained thought space.</li> </ul> <p><strong>3 State evaluator</strong></p> <p>Now with the multiple thoughts generated, state evaluator evaluates the progress they make towards solving the problem. Here the authors proposed two strategies to evaluate (both using LLM):</p> <ul> <li>Value: Rate a state with a scalar (0 to 10), or classification (sure/likely/impossible)</li> <li>Vote: this is when problem success is hard to directly value, then LLM will vote for the most promising one.</li> </ul> <p><strong>4 Search algorithm</strong></p> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-09_at_07.08.00.png" width="70%" alt="Search Algorithms"/></p> <p>Given the increasing size of the tree, the authors proposed two algorithms to search for the most promising path while keeping the tree manageable.</p> <p>BFS explores the problem layer by layer. At each step, it generates candidates, evaluates them, and keeps only the top <em>b</em> most promising states to carry forward. Note that even with b=1 (tree becomes a chain), ToT is more promising than CoT, since each kept thought is evaluated.</p> <p>DFS dives into the single most promising path first. If the state evaluator determines that a current state is “impossible” to solve from (a dead end), DFS actively prunes that subtree and <strong>backtracks</strong> to the parent state to try the next best alternative.</p> <h3 id="example">Example</h3> <p>In the paper, the author consider three experiments, Game24, creative writing, and crosswords. Here we just take the setup of Game24 as an example to enhance our understanding about ToT.</p> <p>Game of 24 is a mathematical reasoning challenge, where the goal is to use 4 numbers and basic arithmetic operations (+-*/) to obtain 24.</p> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-09_at_07.17.04.png" width="70%" alt="Game of 24 Example"/></p> <p>For the thought decomposition, this game has at most 3 steps (4 initial numbers, one fewer after each arithmetic operation). Thought generator in this case is at each step, one LLM call to propose some possible moves. Then one separate LLM call to evaluate each thought candidate as ‘sure/maybe/impossible’ wrt reaching 24. Then BFS is employed to keep the best b=5 candidates at each step.</p> <h3 id="result">Result</h3> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-09_at_09.59.08.png" width="70%" alt="ToT Results"/></p> <p>For the Game24, the result of ToT is outperforming CoT, even with b=1. From the error analysis (fig 3(b)), CoT failed mostly at the first step of the reasoning, then it has no capability to ‘correct itself’. However, ToT fails more ‘equally’ at each step. This is mainly due its ability to explore and self-correct. The final column in fig.3(b) showcased that ToT has much higher success rate compare to CoT (consistent to the result in Table 2).</p> <h1 id="reflexion">Reflexion</h1> <p>This work is considered as the memory part of the ReAct paradigm. Now with ReAct paradigm and Tree of Thought, the latency for the agent to the solve the task can be huge. Some knowledge saving along the process can be very helpful in this case.</p> <p>Reflexion is analogy to basic reinforcement learning setting, but with verbal information as policy improver, or memory. Reflexion converts feedback from the environment (binary or scalar) into verbal feedback (as textual summary). Reflexion uses that verbal information to help agents learn from prior failings by adding it as additional context.</p> <p>Generating these reflective feedback depends on the task. In this paper, the author considered 3 scenarios</p> <ul> <li>decision-making: AlfWorld, a suite of text-based environments that challenge an agent to solve multi-step tasks</li> <li>reasoning: HotpotQA, a Wikipedia-based dataset with QA pairs</li> <li>programming: Python and Rust code writing on MBPP, HumanEval, and LeetcodeHardGym</li> </ul> <p>The following example (HotpotQA) illustrate the workflow. The first trial failed, then reflections is generated by a LLM, which is used as context in trial 2.</p> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-03_at_10.59.53.png" width="80%" alt="Reflexion Example"/></p> <p>The experiment in this paper shows the power of Reflexion by outperforming agents like ReAct in ALFWorld.</p> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-03_at_11.03.23.png" width="70%" alt="Reflexion Performance"/></p> <h2 id="architecture">Architecture</h2> <p><img src="/assets/img/llm-agents/Screenshot_2026-03-03_at_11.07.18.png" width="70%" alt="Reflexion Architecture"/></p> <p>Reflexion is taking ReAct paradigm and has 4 main components (besides the Env): Actor, Evaluator, Self-reflection, and Memory. All the first three are LLMs, which can be different models, or same model different calls. Memory in this work is text.</p> <p><strong>Actor</strong>. This is the same as in ReAct where a LLM is prompted to generate an action. In Figure 2(a), if there is only trajectory as input to Actor, then we fallback to ReAct.</p> <p><strong>Evaluator</strong>. This can be a LLM call as well to assessing the quality of the trajectories so far, or at the end of the trail (case by case).</p> <p><strong>Self-reflection</strong>. This can be a event-triggered LLM call, for instance a failure signal, and write down textual suggestions/guidance as ‘long-term’ memory for the future trials. This process of trial-error-reflection can rapidly improve the agent’s performance, and very consistent with human behavior.</p> <p><strong>Memory</strong>. In this work, there are two types of the memories, short/long-term. The current trajectory of ReAct is short-term and the one from self-reflection is for long. Both are provided as context for the actor.</p> <h1 id="my-take-away">My take away</h1> <p>A truly capable autonomous agent requires all three pillars. It uses <strong>ReAct</strong> to interact meaningfully with the world, <strong>ToT</strong> to deliberately plan its choices through search and self-evaluation, and <strong>Reflexion</strong> to learn from its experiences (failure in Reflexion, but perhaps success can help in other tasks) and improve over time. Together, they transform LLMs from passive text generators into active, deliberate, and continuously learning agents.</p>]]></content><author><name></name></author><category term="paper-reading"/><category term="llm-agents"/><category term="react"/><category term="tree-of-thoughts"/><category term="reflexion"/><category term="reasoning"/><category term="planning"/><category term="memory"/><category term="shunyu-yao"/><summary type="html"><![CDATA[Exploring three foundational frameworks for LLM agents - ReAct (action and reasoning), Tree of Thoughts (planning and search), and Reflexion (memory and learning) - that transform passive language models into capable autonomous agents.]]></summary></entry><entry><title type="html">Mamba: Linear-Time Sequence Modeling with Selective State Spaces</title><link href="https://jq-wei.github.io/blog/2026/mamba-linear-time-sequence-modeling/" rel="alternate" type="text/html" title="Mamba: Linear-Time Sequence Modeling with Selective State Spaces"/><published>2026-01-20T00:00:00+00:00</published><updated>2026-01-20T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2026/mamba-linear-time-sequence-modeling</id><content type="html" xml:base="https://jq-wei.github.io/blog/2026/mamba-linear-time-sequence-modeling/"><![CDATA[<p>By Albert Gu and Tri Dao.</p> <p>LLMs are dominated by transformers nowadays. In this blog post we review a different one called Mamba. Mamba has deep ties to the control community, and leveraging wisdom from system control could have potential to improve research on SSM-based large models.</p> <p>Motivation of Mamba is trying to merge the gap between transformer and a group of more traditional RNN-type models. Essentially, transformer is a type of RNN-type model with expanding hidden state (KV caches), while the traditional RNN-models maintain a fix-size internal state for the whole sequence.</p> <p>Mamba has competitive performance comparing to transformer at small model level and faster inference speed. But Mamba probably will not replace transformers, and it has the potential to be long-term memory component of the future model architecture.</p> <h1 id="basic-formulas">Basic formulas</h1> <p>Everything starts with the classic continuous-time state-space model</p> \[\begin{align*} h'(t) &amp;= Ah(t) + Bx(t) \\ y(t) &amp;= Ch(t) \end{align*}\] <p>where $x(t)\in\mathbb{R}$ and $y(t)\in\mathbb{R}$ are input and output of the system respectively. This system can be seen as a mapping from $x(t)$ to $y(t)$, via hidden state $h(t)\in\mathbb{R}^N$.</p> <p>Continuous-time ODEs can only be computed via discretization, which can be formulated as</p> \[\begin{equation} \begin{aligned} h_t &amp;= \bar{A} h_{t-1} + \bar{B} x_t \\ y_t &amp;= C h_t \end{aligned} \end{equation}\] <p>where</p> \[\begin{align} \bar{A} &amp;= \exp(\Delta A) &amp; \bar{B} &amp;= (\Delta A)^{-1} (\exp(\Delta A) - I) \cdot \Delta B \end{align}\] <p>and $\Delta$ is the step size.</p> <p>Given this discretization (1), the output can be either computed recurrently as in (1), or in convolution as</p> \[\begin{equation} \begin{aligned} \bar{K} &amp;= (C\bar{B}, C\bar{A}\bar{B}, \ldots, C\bar{A}^k \bar{B}, \ldots) \\ y &amp;= x * \bar{K} \end{aligned} \end{equation}\] <p>or more verbosely as</p> \[y_t = C\bar{B}x_t + C\bar{A}\bar{B}x_{t-1} + \ldots + C\bar{A}^{t-1}\bar{B}x_1.\] <p>Advantage of doing this is to save the maintain of internal state $h$ which can save some footprint in vram. The authors also discussed that the for efficient training, the convolution form is preferred, but for inference, it mainly use recurrent form.</p> <p>The classic state-space model is linear time invariant, namely all the matrices are constant which has very limited capability to generalize. Hence the authors proposed the following enhancement of the model architecture.</p> <h2 id="structured-ssm">Structured SSM</h2> <p>Before generalize the weights the SSM (1), the author put one constraint on the matrix $A$ being diagonal for better compute efficiency. Now the matrix $A,B,C$ are essentially N-dim vectors.</p> <p>Typical token sequences $x$ for LLM is of batch size B, length L and (embedding) channels D. In this case, the SSM is applied to each channel separately, hence the dmension of the total hidden states is DN.</p> <h1 id="code-realization">Code realization</h1> <p>The following graph shows the mamba architecture clearly. Here we do a simple inference to understand the flow better. We mainly focus on the class <code class="language-plaintext highlighter-rouge">MambaMixer</code>.</p> <p><img src="/assets/img/mamba1/Screenshot_2026-01-19_at_21.35.19.png" width="70%" alt="Mamba Architecture"/></p> <p>We start with a simple prompt “How are you doing?” with 6 tokens. After embedding layers, the inputs becomes a tensor of shape [1,6, 2560] ([batch_size, seq_len, hidden_size]).</p> <h2 id="step-1-linear-projection">Step 1. Linear projection</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 1. Gated MLP's linear projection
</span><span class="n">projected_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">in_proj</span><span class="p">(</span><span class="n">input_states</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># [batch, 2 * intermediate_size, seq_len]
</span><span class="n">hidden_states</span><span class="p">,</span> <span class="n">gate</span> <span class="o">=</span> <span class="n">projected_states</span><span class="p">.</span><span class="nf">chunk</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>This part corresponds to the beginning two inverted trapezoids. In paper section 3.4, the author discussed about expansion model dimension D by an expanding factor E. Here the hidden_states and gate will travel through left and right path of the architecture, respectively. We will discuss the gate later when it merge back.</p> <p>For prefill stage, the hidden_states and gate are of shape [1, 5120, 6] ([batch_size, intermediate_size, seq_len])</p> <p>For decoding stage, since we generate a new token at a time, the hidden_states is of shape [1, 5120, 1].</p> <h2 id="step-2-convolution">Step 2. Convolution</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">cache_position</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="n">self</span><span class="p">.</span><span class="n">conv_kernel_size</span><span class="p">:</span>  <span class="c1"># PREFILL
</span>    <span class="n">conv_state</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="n">functional</span><span class="p">.</span><span class="nf">pad</span><span class="p">(</span>
        <span class="n">hidden_states</span><span class="p">,</span>
        <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">conv_kernel_size</span> <span class="o">-</span> <span class="n">hidden_states</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">],</span> <span class="mi">0</span><span class="p">)</span>
    <span class="p">)</span>

    <span class="n">cache_params</span><span class="p">.</span><span class="nf">update_conv_state</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span><span class="p">,</span> <span class="n">conv_state</span><span class="p">,</span> <span class="n">cache_position</span><span class="p">)</span>
    <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">conv1d</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)[...,</span> <span class="p">:</span><span class="n">seq_len</span><span class="p">])</span>  <span class="c1"># [batch, intermediate_size, seq_len]
</span><span class="k">else</span><span class="p">:</span> <span class="c1"># DECODING
</span>    <span class="n">conv_state</span> <span class="o">=</span> <span class="n">cache_params</span><span class="p">.</span><span class="nf">update_conv_state</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">,</span> <span class="n">cache_position</span><span class="p">)</span>
    <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">conv_state</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">conv1d</span><span class="p">.</span><span class="n">weight</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">,</span> <span class="p">:],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">use_conv_bias</span><span class="p">:</span>
        <span class="n">hidden_states</span> <span class="o">+=</span> <span class="n">self</span><span class="p">.</span><span class="n">conv1d</span><span class="p">.</span><span class="n">bias</span>
    <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">dtype</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># [batch, intermediate_size, 1] : decoding
</span></code></pre></div></div> <p>In paper section 3.5, the authors motivate the convolution by filtering ‘noise’ in long context. Here this convolution part is also selective in the sense that it is input dependent. In other words, the weights of <code class="language-plaintext highlighter-rouge">self.conv1d</code> are trainable.</p> <p>Here in mamba-2.8B model, a convolution kernel of size 4 (conv_kernel_size) is used. For our input sequence which is longer than 4, padding is not needed.</p> <p>For prefill, the hidden_states after convolution is of shape [1, 5120, 6] ([batch, intermediate_size, seq_len]). The convolution is essentially ‘smooth’ the input sequence.</p> <p>For decoding, the hidden_states is of shape [1, 5120, 1] before convolution, then the convolution is done with last 4 tokens of the current sequence to generate the output for the current single token. Hence hidden_states after convolution is kept as [1, 5120, 1].</p> <h2 id="step-3-ssm-recurrent">Step 3. SSM recurrent</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 3. State Space Model sequence transformation
# 3.a. Selection:  [batch, seq_len, self.time_step_rank + self.ssm_state_size * 2]
</span><span class="n">ssm_parameters</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">x_proj</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span>
<span class="n">time_step</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">C</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span>
    <span class="n">ssm_parameters</span><span class="p">,</span> <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">time_step_rank</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">ssm_state_size</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">ssm_state_size</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span>
<span class="p">)</span>
<span class="n">discrete_time_step</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dt_proj</span><span class="p">(</span><span class="n">time_step</span><span class="p">)</span>                                    <span class="c1"># [batch, seq_len, intermediate_size]
</span><span class="n">discrete_time_step</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="n">functional</span><span class="p">.</span><span class="nf">softplus</span><span class="p">(</span><span class="n">discrete_time_step</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># [batch, intermediate_size, seq_len]
</span>
<span class="c1"># 3.b. Discretization: B and C to [batch, seq_len, intermediate_size, ssm_state_size] (SRAM)
</span><span class="n">A</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">A_log</span><span class="p">.</span><span class="nf">float</span><span class="p">())</span>                                              <span class="c1"># [intermediate_size, ssm_state_size]
</span><span class="n">discrete_A</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">A</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span> <span class="o">*</span> <span class="n">discrete_time_step</span><span class="p">[:,</span> <span class="p">:,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">])</span> <span class="c1"># [batch, intermediate_size, seq_len, ssm_state_size]
</span><span class="n">discrete_B</span> <span class="o">=</span> <span class="n">discrete_time_step</span><span class="p">[:,</span> <span class="p">:,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">B</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:,</span> <span class="p">:].</span><span class="nf">float</span><span class="p">()</span>       <span class="c1"># [batch, intermediate_size, seq_len, ssm_state_size]
</span><span class="n">deltaB_u</span> <span class="o">=</span> <span class="n">discrete_B</span> <span class="o">*</span> <span class="n">hidden_states</span><span class="p">[:,</span> <span class="p">:,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">float</span><span class="p">()</span>

<span class="c1"># 3.c perform the recurrence y ← SSM(A, B, C)(x)
</span><span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">use_mambapy</span> <span class="ow">and</span> <span class="n">self</span><span class="p">.</span><span class="n">training</span> <span class="ow">and</span> <span class="n">cache_params</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
    <span class="bp">...</span>
<span class="k">else</span><span class="p">:</span>
    <span class="n">scan_outputs</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">seq_len</span><span class="p">):</span>
        <span class="n">ssm_state</span> <span class="o">=</span> <span class="n">discrete_A</span><span class="p">[:,</span> <span class="p">:,</span> <span class="n">i</span><span class="p">,</span> <span class="p">:]</span> <span class="o">*</span> <span class="n">ssm_state</span> <span class="o">+</span> <span class="n">deltaB_u</span><span class="p">[:,</span> <span class="p">:,</span> <span class="n">i</span><span class="p">,</span> <span class="p">:]</span>      <span class="c1"># [batch, intermediade_size, ssm_state]
</span>        <span class="n">scan_output</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">ssm_state</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">dtype</span><span class="p">),</span> <span class="n">C</span><span class="p">[:,</span> <span class="n">i</span><span class="p">,</span> <span class="p">:].</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>  <span class="c1"># [batch, intermediade_size, 1]
</span>        <span class="n">scan_outputs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">scan_output</span><span class="p">[:,</span> <span class="p">:,</span> <span class="mi">0</span><span class="p">])</span>
    <span class="n">scan_output</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">(</span><span class="n">scan_outputs</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                                <span class="c1"># [batch, seq_len, intermediade_size]
</span>    <span class="n">scan_output</span> <span class="o">=</span> <span class="n">scan_output</span> <span class="o">+</span> <span class="p">(</span><span class="n">hidden_states</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">D</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">])</span>
    <span class="n">scan_output</span> <span class="o">=</span> <span class="p">(</span><span class="n">scan_output</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">gate</span><span class="p">))</span>
</code></pre></div></div> <p>The is the core part of Mamba, which updates the $h$ according to selective structured SSM (equation (1) in this blog). The first step (3.a) here is to derive the input-dependent (selective) parameters for SSM, namely $\Delta, B, C$. In equation (1), The parameter $\Delta$ controls how much the cuurent input $x_t$ contributes to the SSM state. The authors also use Theorem 1 to motivate that $\Delta$ can be seen as the ‘gating’ mechanism in the traditional RNN domain. In other words, for a special case (scalar integrator), the update of SSM state is weighted average between the previous state and input, and $\Delta$ controls the weight.</p> <p><strong>Selective vs Learnable.</strong> The authors discuss thoroughly about selective mechanism. In short, a variable is selective means it is learnable and input dependent. From the code point of view, the weight matrix A is learnable via</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">A_log</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Parameter</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">A</span><span class="p">))</span>
</code></pre></div></div> <p>but weight B, is derived through <code class="language-plaintext highlighter-rouge">x_proj</code> which is learnable wrt input (<code class="language-plaintext highlighter-rouge">hidden_states</code>)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ssm_parameters</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">x_proj</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span>
<span class="n">time_step</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">C</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span>
    <span class="n">ssm_parameters</span><span class="p">,</span> <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">time_step_rank</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">ssm_state_size</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">ssm_state_size</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span>
<span class="p">)</span>
</code></pre></div></div> <p>After discretization of the SSM model, the SSM state is updated recurrently. Note that system is maintaining a central SSM state only. The shape of the SSM state of this model is [1, 5120, 16] ([batch_size, intermediate_size, state_size]). The way it updates differentiate by prefill and decoding phases. For prefill, <code class="language-plaintext highlighter-rouge">seq_len = 6</code> (len of our prompt), then the previous (slow) implementation update the SSM state iteratively. For decoding, <code class="language-plaintext highlighter-rouge">seq_len = 1</code> which is only for the newly added token.</p> <h2 id="step-4-output-merge">Step 4. Output merge</h2> <p>After the recurrent selective SSM, the output are merged with 1. an (internal) residual path from input with (learnable) weights D; 2. gated input</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scan_output</span> <span class="o">=</span> <span class="n">scan_output</span> <span class="o">+</span> <span class="p">(</span><span class="n">hidden_states</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">D</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">])</span>
<span class="n">scan_output</span> <span class="o">=</span> <span class="p">(</span><span class="n">scan_output</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">gate</span><span class="p">))</span>
</code></pre></div></div> <p>Here is the gated path is motivated by transformers MLP layer (for example <code class="language-plaintext highlighter-rouge">LlamaMLP</code>). But instead of put gated path after attention block as in transformer, Mamba here take it parallel to SSM. One motivation is to do ‘channel’ filtering (wrt embedding-dim). But could be also simply be that MLP has been working very well in transformers which becomes very risky to skip.</p> <h1 id="hardware-opt">Hardware opt</h1> <p>This paper is written by the author of flash attention, so the hardware efficiency is guaranteed as well. The main idea to keep the heavy lifting of discretization and SSM update in SRAM which is closer to compute unit, while keep the weights $\Delta, A, B, C$ in the HBM and only write back the output y to HBM. This is possible due to the SSM update are independent wrt channels, and can be parallelized by scan algorithm (<em>Blelloch 1990; Martin and Cundy 2018; Smith, Warrington, and Linderman 2023</em>).</p> <h1 id="conclusion">Conclusion</h1> <p><img src="/assets/img/mamba1/Screenshot_2026-01-20_at_11.38.39.png" width="70%" alt="Mamba Recurrent Update"/></p> <p>Now this picture makes perfect sense: as the sequence goes on, the latest token is used to update the SSM state recurrently, which will be used further on to generate the next token, and so on.</p> <p>Since Mamba and transformers are both originated from classic RNN, and they have similarity of their internal tensors, there has been some research showing that some weights of pretrained transformers like Llama can be borrowed to initialize some of the Mamba weights, which in turn saves the training time and gain better performance.</p> <p>Both advantage and disadvantage of Mamba are pretty clear: it has a fix-size internal state to maintain for infinite-long sequences, which is more efficient transformers with expanding internal states; but because of this information compression, it will loose the details of the long sequences.</p> <h1 id="dictionary">Dictionary</h1> <p>Here we collect some key parameters and variable definition from the paper and code. We are using <code class="language-plaintext highlighter-rouge">mamba-2.8b</code> for code reference.</p> <p>This model has 64 hidden layers.</p> <ul> <li><strong>hidden_size</strong>: 2560 (embedding dim, D in paper)</li> <li><strong>intermediate_size</strong>: 5120 (= 2x hidden_size, ED in paper)</li> <li><strong>state_size</strong>: 16 (N in paper, config.state_size in code)</li> <li><strong>conv_kernel</strong>: 4</li> <li><strong>vocab_size</strong>: 50280</li> <li><strong>hidden_state</strong>: x in paper</li> </ul>]]></content><author><name></name></author><category term="paper-reading"/><category term="mamba"/><category term="state-space-models"/><category term="ssm"/><category term="rnn"/><category term="transformers"/><category term="sequence-modeling"/><category term="deep-learning"/><summary type="html"><![CDATA[Notes on Mamba paper by Albert Gu and Tri Dao - exploring selective state space models as an alternative to transformers with fixed-size internal states and efficient inference.]]></summary></entry><entry><title type="html">mHC: Manifold-Constrained Hyper-Connections</title><link href="https://jq-wei.github.io/blog/2026/mhc-manifold-constrained-hyper-connections/" rel="alternate" type="text/html" title="mHC: Manifold-Constrained Hyper-Connections"/><published>2026-01-08T00:00:00+00:00</published><updated>2026-01-08T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2026/mhc-manifold-constrained-hyper-connections</id><content type="html" xml:base="https://jq-wei.github.io/blog/2026/mhc-manifold-constrained-hyper-connections/"><![CDATA[<h1 id="abstract">Abstract</h1> <p>The key points of this paper are</p> <ul> <li>residual connection is ubiquitous. This is a paradigm since ResNet?</li> <li>Hyper-Connection extend residual connection <ul> <li>can have substantial performance gain</li> <li>but unstable for training</li> <li>hard to scale</li> <li>more memory access overhead</li> <li>this is because HC compromise residual connection’s identity mapping property</li> </ul> </li> <li>Solution: manifold constrained HC by projecting HC onto a manifold to ensure stability <img src="/assets/img/mhc/Screenshot_2026-01-07_at_23.03.03.png" width="60%" alt="Screenshot 2026-01-07 at 23.03.03.png"/></li> </ul> <h1 id="main-method">Main Method</h1> <p>The residual connection has been there for deep learning for long time, but has been kept it original form so far. A (probably any) single-layer neural network can be typically formulated as</p> \[\mathbf{x}_{l+1} = \mathbf{x}_{l} + \mathcal{F}(\mathbf{x}_{l}, \mathcal{W}_l)\] <p>For simplicity, we can think of $\mathbf{x}_l \in\mathbb{R}^C$. This corresponding to the Transformer with model dimension being $1$.</p> <p>In fact, for any RNN type of neural network, $\mathbf{x}_l$ is the hidden states of the existing $C$ tokens.</p> <p>The function $\mathcal{F}(\cdot, \mathcal{W}_{l})$ can be simple matrix multiplication like MLP, or more complicated calculation like attention mechanisms.</p> <p>Note that when we have multiple layers, the network becomes</p> \[\mathbf{x}_{L} = \mathbf{x}_{l} + \sum_{i=l}^{L-1}\mathcal{F}(\mathbf{x}_{i}, \mathcal{W}_i)\] <p>namely the input states can come all the way till the end of the network.</p> <h2 id="hyper-connections">Hyper-Connections</h2> <p>This paper is mainly motivated by the paper Hyper-Connections (from Bytedance). The main idea to expand the residual connection, namely one more dimension to the hidden states, and enhance the connection complexity.</p> <p>Again for simplicity, we take the expansion from 1-D vector to a 2-D matrix, but it can be arbitrary. The formula for HC is</p> \[\mathbf{x}_{l+1} = \mathcal{H}_{l}^{\text{res}} \mathbf{x}_{l} + \mathcal{H}_{l}^{\text{post}^\top} \mathcal{F}(\mathcal{H}_{l}^{\text{pre}} \mathbf{x}_{l}, \mathcal{W}_{l})\] <p>where we have</p> <ul> <li>$\mathbf{x}_l\in\mathbb{R}^{n\times C}$ is the expanded hidden state and $n$ is the expansion rate</li> <li>$\mathcal{H}_{l}^{\text{res}}\in\mathbb{R}^{n\times n}$ is a learnable mapping as feature mixture in the residual connection</li> </ul> <p>In order to keep the remaining part of the neural network unchanged, we need to map $\mathbf{x}_l$ in $\mathcal{F}$ to match the weights $\mathcal{W}_l$. This introduces two more learnable mappings: \(\mathcal{H}_{l}^{\text{pre}}, \mathcal{H}_{l}^{\text{post}}\in\mathbb{R}^{1\times n}\).</p> <p>Now the multiple layers network with HC becomes</p> \[\mathbf{x}_{L} = \left( \prod_{i=1}^{L-l} \mathcal{H}_{L-i}^{\text{res}} \right) \mathbf{x}_{l} + \sum_{i=l}^{L-1} \left( \prod_{j=1}^{L-1-i} \mathcal{H}_{L-j}^{\text{res}} \right) \mathcal{H}_{i}^{\text{post}^\top} \mathcal{F}(\mathcal{H}_{i}^{\text{pre}} \mathbf{x}_{i}, \mathcal{W}_{i})\] <p>Note that HC has parameterized implementation as</p> \[\begin{cases}\tilde{\mathbf{x}}_{l} = \text{RMSNorm}(\mathbf{x}_{l}) \\\mathcal{H}_{l}^{\text{pre}} = \alpha_{l}^{\text{pre}} \cdot \tanh(\theta_{l}^{\text{pre}} \tilde{\mathbf{x}}_{l}^{\top}) + \mathbf{b}_{l}^{\text{pre}} \\\mathcal{H}_{l}^{\text{post}} = \alpha_{l}^{\text{post}} \cdot \tanh(\theta_{l}^{\text{post}} \tilde{\mathbf{x}}_{l}^{\top}) + \mathbf{b}_{l}^{\text{post}} \\\mathcal{H}_{l}^{\text{res}} = \alpha_{l}^{\text{res}} \cdot \tanh(\theta_{l}^{\text{res}} \tilde{\mathbf{x}}_{l}^{\top}) + \mathbf{b}_{l}^{\text{res}},\end{cases}\] <p>where:</p> <ul> <li>$\alpha \in \mathbb{R}$ are learnable, initialized to small values</li> <li>$\theta_{l}^{\text{pre}}, \theta_{l}^{\text{post}} \in \mathbb{R}^{1 \times C}$ and $\theta_{l}^{\text{res}} \in \mathbb{R}^{n \times C}$</li> <li>$\mathbf{b}_{l}^{\text{pre}}, \mathbf{b}_{l}^{\text{post}} \in \mathbb{R}^{1 \times n}$</li> <li>$\mathbf{b}_{l}^{\text{res}} \in \mathbb{R}^{n \times n}$ are learnable bias which are initialized to zero (usually).</li> </ul> <p>The author also did an ablation study of HC, and confirms that the expanded residual connection contribute the most to the performance gain.</p> <p>The contribution of HC is that it not only propose a new residual connection with more stream, but increase the hidden state dimension which can potentially contain more information about a sequence.</p> <p><strong>The problem of HC</strong> is also obvious: it introduces instability with $\prod_{i=1}^{L-l} \mathcal{H}_{L-i}^{\text{res}}$, the elements can be both positive and negative, the eigenvalues are not constrainned etc. Moreover, during the backward propagation, the gradient gain becomes very large (nearly 3000).</p> <h2 id="mhc">mHC</h2> <p>For the system theory and Markov chains, we learned that for autonomous system, governed by a double stochastic matrix, is Lyapunov stable. This is another motivation to the solution proposed by the author. The idea is to map the matrix $\mathcal{H}_{l}^{\text{res}}$ to manifold of stochastic matrices</p> \[\mathcal{P}_{\mathcal{M}^{\text{res}}}(\mathcal{H}_{l}^{\text{res}}) := \left\{ \mathcal{H}_{l}^{\text{res}} \in \mathbb{R}^{n \times n} \mid \mathcal{H}_{l}^{\text{res}} \mathbf{1}_{n} = \mathbf{1}_{n}, \, \mathbf{1}_{n}^{\top} \mathcal{H}_{l}^{\text{res}} = \mathbf{1}_{n}^{\top}, \, \mathcal{H}_{l}^{\text{res}} \geqslant 0 \right\}\] <p>Note that this manifold is equivalent to the convex hull of all n by n permutation matrix. Also, if the expansion rate $n = 1$, then the manifold is degenerated to $1$, and the residual connection become the classic identity one.</p> <p>The actual implementation is given as follows.</p> <ol> <li>Start with parameterization</li> </ol> \[\begin{cases}\vec{\mathbf{x}}'_{l} = \text{RMSNorm}(\vec{\mathbf{x}}_{l}) \\\tilde{\mathcal{H}}_{l}^{\text{pre}} = \alpha_{l}^{\text{pre}} \cdot (\vec{\mathbf{x}}'_{l} \varphi_{l}^{\text{pre}}) + \mathbf{b}_{l}^{\text{pre}} \\\tilde{\mathcal{H}}_{l}^{\text{post}} = \alpha_{l}^{\text{post}} \cdot (\vec{\mathbf{x}}'_{l} \varphi_{l}^{\text{post}}) + \mathbf{b}_{l}^{\text{post}} \\\tilde{\mathcal{H}}_{l}^{\text{res}} = \alpha_{l}^{\text{res}} \cdot \text{mat}(\vec{\mathbf{x}}'_{l} \varphi_{l}^{\text{res}}) + \mathbf{b}_{l}^{\text{res}},\end{cases}\] <p>where:</p> <ul> <li>$\vec{\mathbf{x}}_{l}=\text{vec}(\mathbf{x}_{l})\in\mathbb{R}^{1 \times nC}$ is the flattened vector</li> <li>$\varphi_{l}^{\text{pre}}, \varphi_{l}^{\text{post}} \in \mathbb{R}^{nC \times n}$ and $\varphi_{l}^{\text{res}} \in \mathbb{R}^{nC \times n^{2}}$ are learnable weights</li> <li>$\text{mat}$ is a reshape func from $\mathbb{R}^{1 \times n^{2}}$ to $\mathbb{R}^{n \times n}$</li> </ul> <ol> <li>Then do the constrained mapping</li> </ol> \[\begin{cases}\mathcal{H}_{l}^{\text{pre}} = \sigma(\tilde{\mathcal{H}}_{l}^{\text{pre}}) \\\mathcal{H}_{l}^{\text{post}} = 2\sigma(\tilde{\mathcal{H}}_{l}^{\text{post}}) \\\mathcal{H}_{l}^{\text{res}} = \text{Sinkhorn-Knopp}(\tilde{\mathcal{H}}_{l}^{\text{res}}),\end{cases}\] <p>where $\sigma$ is the sigmoid function and the $\text{Sinkhorn-Knopp}$ operator is working as the following:</p> <ol> <li> <p>initialize with</p> \[\mathbf{M}^{(0)} = \text{exp}(\tilde{\mathcal{H}}_{l}^{\text{res}})\] </li> <li> <p>iterate</p> \[\mathbf{M}^{(t)} = \mathcal{T}_{r}\left( \mathcal{T}_{c}(\mathbf{M}^{(t-1)}) \right)\] <p>until converge, where $\mathcal{T}<em>{r}, \mathcal{T}</em>{c}$ are row and column normalization, respectively.</p> </li> <li> <p>denote the final result as</p> \[\mathcal{H}_{l}^{\text{res}} = \lim_{t\rightarrow \infty} \mathbf{M}^{(t)}\] <p>Practically, take $t_{\max} =20$.</p> </li> </ol> <p>The code implementation of $\text{Sinkhorn-Knopp}$ opt can be as</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="k">def</span> <span class="nf">sinkhorn_knopp_torch</span><span class="p">(</span><span class="n">X</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">t_max</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">20</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="n">M</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">t_max</span><span class="p">):</span>
        <span class="n">M</span> <span class="o">=</span> <span class="n">M</span> <span class="o">/</span> <span class="n">M</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">M</span> <span class="o">=</span> <span class="n">M</span> <span class="o">/</span> <span class="n">M</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">M</span>
</code></pre></div></div> <p>The benefits of using double stochastic $\mathcal{H}_{l}^{\text{post}}$ are norm preservation which effectively solve the gradient explosion problem; and the residual connection can be seen as a convex combination of information across streams, like a feature fusion mechanism.</p> <h3 id="why-2-">why 2 ?</h3> <p>In the mapping of mHC, the factor 2 in post projection is to make the $\mathcal{H}_{l}^{\text{post}}$ close to uniform matrix.</p> <p>This happens when the elements of $\tilde{\mathcal{H}}_{l}^{\text{post}}$ are close to zero for example at initialization. This is because $\sigma(0) = 0.5$.</p> <h1 id="performance">Performance</h1> <p>The author did a heaving lifting by implement the kernel fusion to make the training efficient, and experiment with 27B model.</p> <p>Here are the main results</p> <p><strong>mHC gives better performance in term of metrics and stability</strong></p> <p><img src="/assets/img/mhc/Screenshot_2026-01-08_at_22.16.36.png" width="60%" alt="Screenshot 2026-01-08 at 22.16.36.png"/></p> <p>In this figure, we can see that mHC gives the most stable training with the least loss. This is also verified by testing on some benchmarks. mHC outperform the other method in most of the cases.</p> <p><img src="/assets/img/mhc/Screenshot_2026-01-08_at_22.18.57.png" width="60%" alt="Screenshot 2026-01-08 at 22.18.57.png"/></p> <h1 id="my-takeaway">My takeaway</h1> <p>This is a promising method to expand the overlooked residual connection, which can further more improve of the performance of the Transformer at least. It basically expands the dimension of the hidden state of a neural network. Considering the trend of dimension expansion along RNN (from finite-dimensional hidden states, classic RNN, LSTM, Mamba etc, to infinite-dimensional ones like Transformer), this work opens another perspective to scale.</p> <p>mHC introduce minor modifications to the other part of the neural network. Except for the Transformer, embedding layers needs to be expanded as well.</p>]]></content><author><name></name></author><category term="paper-reading"/><category term="deep-learning"/><category term="residual-connections"/><category term="hyper-connections"/><category term="transformer"/><category term="stability"/><category term="deepseek"/><summary type="html"><![CDATA[Notes on DeepSeek's mHC paper - extending residual connections with Hyper-Connections constrained on double stochastic matrix manifolds for stability. Covers implementation with Sinkhorn-Knopp algorithm and performance improvements.]]></summary></entry><entry><title type="html">ReAct: Synergizing Reasoning and Acting in Language Models</title><link href="https://jq-wei.github.io/blog/2026/react-synergizing-reasoning-acting/" rel="alternate" type="text/html" title="ReAct: Synergizing Reasoning and Acting in Language Models"/><published>2026-01-01T00:00:00+00:00</published><updated>2026-01-01T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2026/react-synergizing-reasoning-acting</id><content type="html" xml:base="https://jq-wei.github.io/blog/2026/react-synergizing-reasoning-acting/"><![CDATA[<p>This is the most cited work by Shunyu Yao so far.</p> <p>This paper is essentially about prompt engineering, but defines a paradigm of interacting with LLM, especially with explosion of LLM agent. In fact, more recent LLMs have reasoning capability as builtin capability, which essentially means ReAct is enabled by default. ReAct enjoys several unique features:</p> <ol> <li>Intuitive and easy to design: it is close to human way of solving problems. In the prompt, just need the human annotator write down their thoughts during problem-solving.</li> <li>Less hallucination comparing to Chain-of-thought.</li> </ol> <h1 id="core-idea-of-react">Core idea of ReAct</h1> <p>The basic interaction of an agent with an environment for a task is composed by</p> <ul> <li>at time $t$, agent receives an observation $o_t\in \mathcal{O}$ from the env</li> <li> <table> <tbody> <tr> <td>takes an action $a_t\in\mathcal{A}$ following some policy $\pi(a_t</td> <td>c_t)$</td> </tr> </tbody> </table> </li> <li>$c_t = (o_1, a_1, \ldots, o_{t-1}, a_{t-1}, o_t)$ is the context to the agent.</li> </ul> <p>The authors proposed a very reasonable and intuitive expansion: augment the action space as $\hat{\mathcal{A}} = \mathcal{A}\cup \mathcal{L}$ , where $\mathcal{L}$ is the space of language (for thought and reasoning). Now, besides the action and observation, the full interaction between an agent and an env is expanded with ‘thought’ which is</p> <ul> <li>free-form language thoughts</li> <li>decompose goals,</li> <li>track progress</li> <li>handle exceptions</li> </ul> <h1 id="prompt-design">Prompt design</h1> <p>This paper proposed some prompt templates for different use case. Note all these cases is using few-shot prompt, namely no special prefix design but including some examples in the prompt.</p> <h3 id="hotpotqa">HotpotQA</h3> <p><strong>Dense thoughts</strong>: this eval set is more knowledge intensive, hence a thought accompanies every action to guide the search and synthesis of the info.</p> <p>One example is attached as following:</p> <p><img src="/assets/img/react/Screenshot_2025-12-26_at_10.07.08.png" width="60%" alt="Screenshot 2025-12-26 at 10.07.08.png"/></p> <p><img src="/assets/img/react/Screenshot_2025-12-26_at_10.07.42.png" width="60%" alt="Screenshot 2025-12-26 at 10.07.42.png"/></p> <p><img src="/assets/img/react/Screenshot_2025-12-26_at_10.07.58.png" width="60%" alt="Screenshot 2025-12-26 at 10.07.58.png"/></p> <p>this type is for dense thought, for sparse thought, the model dose not need to think at every step, but asynchronously as needed.</p> <h1 id="why-react-can-be-learned-by-llm">Why ReAct can be learned by LLM?</h1> <p>First, this paper discussed fine-tune a LLM to boost the ReAct performance. The author used a base model PaLM-8/62B as a candidate. Without finetuning, ReAct perform the worst. But with only 3000-example fine-tuning, the ReAct outperform all the others.</p> <p><img src="/assets/img/react/Screenshot_2025-12-30_at_10.48.54.png" width="60%" alt="Screenshot 2025-12-30 at 10.48.54.png"/></p> <p>The authors also compared the ReAct Performance of PaLM-540B and GPT3. GPT3 is better means the model there can follow the ReAct prompt better than PaLM-540B. This is probably because the instruct fine tuning done towards GPT3 with some human thinking and action steps in the samples.</p> <p><img src="/assets/img/react/Screenshot_2025-12-30_at_13.02.41.png" width="60%" alt="Screenshot 2025-12-30 at 13.02.41.png"/></p> <h1 id="in-context-learning-and-zero-shot-react-agent">In-context-learning and zero-shot ReAct agent</h1> <p>In this paper, the authors are using standard in-context-learning with few-shot examples in the prompt for the LLM to learn ReAct. Since then it has become a foundational paradigm for LLM agents. Now in langchain, it provide a zero-shot ReAct agent (Langchain incorporate agent endpoint to <code class="language-plaintext highlighter-rouge">create_agent</code> now, and zero-shot ReAct agent <code class="language-plaintext highlighter-rouge">create_react_agent</code> is moved to <code class="language-plaintext highlighter-rouge">langchain_classic</code>). In this section, we explore some behaviors of this type of agent.</p> <p>The skeleton of the code is as follows, where we defined a zero-shot ReAct agent with 3 basic tools.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain_core.tools</span> <span class="kn">import</span> <span class="n">tool</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">PromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_classic.agents</span> <span class="kn">import</span> <span class="n">create_react_agent</span><span class="p">,</span> <span class="n">AgentExecutor</span>

<span class="c1"># Load environment variables from .env
</span><span class="nf">load_dotenv</span><span class="p">()</span>
<span class="c1"># ============================================================================
# Define Tools
# ============================================================================
</span>
<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">search_wikipedia</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Search Wikipedia for information about people, places, events, concepts.</span><span class="sh">"""</span>
    <span class="kn">import</span> <span class="n">wikipediaapi</span>
    <span class="n">wiki</span> <span class="o">=</span> <span class="n">wikipediaapi</span><span class="p">.</span><span class="nc">Wikipedia</span><span class="p">(</span><span class="n">user_agent</span><span class="o">=</span><span class="sh">'</span><span class="s">ReActTestAgent/1.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">language</span><span class="o">=</span><span class="sh">'</span><span class="s">en</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">page</span> <span class="o">=</span> <span class="n">wiki</span><span class="p">.</span><span class="nf">page</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">page</span><span class="p">.</span><span class="nf">exists</span><span class="p">():</span>
        <span class="k">return</span> <span class="n">page</span><span class="p">.</span><span class="n">summary</span><span class="p">[:</span><span class="mi">500</span><span class="p">]</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">No Wikipedia page found for </span><span class="sh">'</span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="sh">'</span><span class="s">.</span><span class="sh">"</span>

<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">calculate</span><span class="p">(</span><span class="n">expression</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Evaluate a mathematical expression. Examples: </span><span class="sh">'</span><span class="s">2 + 2</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">10 * 5</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">(100 - 20) / 4</span><span class="sh">'</span><span class="s">.</span><span class="sh">"""</span>
    <span class="n">allowed_chars</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="sh">"</span><span class="s">0123456789+-*/(). </span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="nf">all</span><span class="p">(</span><span class="n">c</span> <span class="ow">in</span> <span class="n">allowed_chars</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">expression</span><span class="p">):</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">Error: Only basic math operations allowed</span><span class="sh">"</span>
    <span class="k">return</span> <span class="nf">str</span><span class="p">(</span><span class="nf">eval</span><span class="p">(</span><span class="n">expression</span><span class="p">))</span>

<span class="nd">@tool</span>
<span class="k">def</span> <span class="nf">get_current_year</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Get the current year.</span><span class="sh">"""</span>
    <span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>
    <span class="k">return</span> <span class="nf">str</span><span class="p">(</span><span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">().</span><span class="n">year</span><span class="p">)</span>
<span class="c1"># ============================================================================
# Create Agent
# ============================================================================
</span>
<span class="k">def</span> <span class="nf">create_agent</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">Create a ReAct agent with explicit reasoning traces.</span><span class="sh">"""</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">search_wikipedia</span><span class="p">,</span> <span class="n">calculate</span><span class="p">,</span> <span class="n">get_current_year</span><span class="p">]</span>
    <span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>

    <span class="c1"># ReAct prompt template from the paper
</span>    <span class="n">template</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Answer the following questions as best you can. You have access to the following tools:

{tools}

Use the following format:

Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take, should be one of [{tool_names}]
Action Input: the input to the action
Observation: the result of the action
</span><span class="gp">...</span> <span class="p">(</span><span class="n">this</span> <span class="n">Thought</span><span class="o">/</span><span class="n">Action</span><span class="o">/</span><span class="n">Action</span> <span class="n">Input</span><span class="o">/</span><span class="n">Observation</span> <span class="n">can</span> <span class="n">repeat</span> <span class="n">N</span> <span class="n">times</span><span class="p">)</span>
<span class="n">Thought</span><span class="p">:</span> <span class="n">I</span> <span class="n">now</span> <span class="n">know</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span>
<span class="n">Final</span> <span class="n">Answer</span><span class="p">:</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span> <span class="n">to</span> <span class="n">the</span> <span class="n">original</span> <span class="nb">input</span> <span class="n">question</span>

<span class="s">Begin!

Question: {input}
Thought:{agent_scratchpad}</span><span class="sh">"""</span>

    <span class="n">prompt</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="n">template</span><span class="p">)</span>
    <span class="n">agent</span> <span class="o">=</span> <span class="nf">create_react_agent</span><span class="p">(</span><span class="n">llm</span><span class="p">,</span> <span class="n">tools</span><span class="p">,</span> <span class="n">prompt</span><span class="p">)</span>

    <span class="k">return</span> <span class="nc">AgentExecutor</span><span class="p">(</span>
        <span class="n">agent</span><span class="o">=</span><span class="n">agent</span><span class="p">,</span>
        <span class="n">tools</span><span class="o">=</span><span class="n">tools</span><span class="p">,</span>
        <span class="n">verbose</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">handle_parsing_errors</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">max_iterations</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
        <span class="n">return_intermediate_steps</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>
<span class="c1"># ============================================================================
# Main
# ============================================================================
</span>
<span class="k">def</span> <span class="nf">main</span><span class="p">():</span>

    <span class="n">agent</span> <span class="o">=</span> <span class="nf">create_agent</span><span class="p">()</span>

    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">question</span> <span class="o">=</span> <span class="nf">input</span><span class="p">(</span><span class="sh">"</span><span class="s">Question: </span><span class="sh">"</span><span class="p">).</span><span class="nf">strip</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">question</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">()</span>
            <span class="n">result</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span> <span class="n">question</span><span class="p">})</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">Final Answer: </span><span class="si">{</span><span class="n">result</span><span class="p">[</span><span class="sh">'</span><span class="s">output</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="nf">main</span><span class="p">()</span>
</code></pre></div></div> <p>If we ask the agent “If Wolfgang Amadeus Mozart had lived as long as Ludwig van Beethoven did, what year would Mozart have died?”, we got the following chain:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reasoning: First, I need to find out when Mozart was born and when he died to calculate his age. Then, I need to find out when Beethoven was born and when he died to calculate his age. If Mozart had lived as long as Beethoven, I can add Beethoven's age to Mozart's birth year to find out the year Mozart would have died.
Action: search_wikipedia
Action Input: "Wolfgang Amadeus Mozart"
Observation: Wolfgang Amadeus Mozart (27 January 1756 – 5 December 1791) was a prolific and influential composer of the Classical period. Despite his short life, his rapid pace of composition and proficiency from an early age resulted in more than 800 works representing virtually every Western classical genre of his time. Many of these compositions are acknowledged as pinnacles of the symphonic, concertante, chamber, opera, and choral repertoires. Mozart is widely regarded as one of the greatest composers in
Reasoning: Now I know that Mozart was born in 1756 and died in 1791. I need to calculate his age.
Action: calculate
Action Input: "1791 - 1756"
Observation: 35
Reasoning: Mozart was 35 years old when he died. Now I need to find out when Beethoven was born and when he died to calculate his age.
Action: search_wikipedia
Action Input: "Ludwig van Beethoven"
Observation: Ludwig van Beethoven (baptised 17 December 1770 – 26 March 1827) was a German composer and pianist. One of the most revered figures in the history of Western music, his works rank among the most performed of the classical music repertoire and span the transition from the Classical period to the Romantic era. Beethoven's early period, during which he forged his craft, is typically considered to have lasted until 1802. From 1802 to around 1812, his middle period showed an individual development fr
Reasoning: Now I know that Beethoven was born in 1770 and died in 1827. I need to calculate his age.
Action: calculate
Action Input: "1827 - 1770"
Observation: 57
Reasoning: Beethoven was 57 years old when he died. Now, if Mozart had lived as long as Beethoven, I can add Beethoven's age to Mozart's birth year to find out the year Mozart would have died.
Action: calculate
Action Input: "1756 + 57"
Observation: 1813
Reasoning: I now know the final answer
Final Answer: If Wolfgang Amadeus Mozart had lived as long as Ludwig van Beethoven did, Mozart would have died in the year 1813.
</code></pre></div></div>]]></content><author><name></name></author><category term="paper-reading"/><category term="react"/><category term="llm-agents"/><category term="prompt-engineering"/><category term="reasoning"/><category term="chain-of-thought"/><category term="langchain"/><summary type="html"><![CDATA[Notes on ReAct - a paradigm for LLM agents that combines reasoning traces and task-specific actions. Covers prompt engineering, fine-tuning approaches, and implementation of zero-shot ReAct agents with LangChain.]]></summary></entry><entry><title type="html">A Survey on In-Context Learning</title><link href="https://jq-wei.github.io/blog/2025/in-context-learning-survey/" rel="alternate" type="text/html" title="A Survey on In-Context Learning"/><published>2025-12-18T00:00:00+00:00</published><updated>2025-12-18T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2025/in-context-learning-survey</id><content type="html" xml:base="https://jq-wei.github.io/blog/2025/in-context-learning-survey/"><![CDATA[<p>This paper is from Peking U, ByteDance, Shanghai AI Lab, Alibaba.</p> <p>Nowadays in-context-learning become a good practice when we use LLM to solve real-world task with some pattern or style. This paper is a good entry point to understand the research landscape of ICL (in context learning).</p> <p>In this note I record my learning note on this domain, with deep dive into some papers in the reference.</p> <p>The key idea of in-context learning is to learn from analogy (类比) usually with a few examples in the prompt.</p> <p>ICL does not perform parameter updates. The model is expected to learn the pattern hidden in the demonstration and accordingly make the right prediction</p> <h1 id="advantages-of-icl">Advantages of ICL</h1> <ol> <li>It is much easier to incorporate human knowledge into LLMs by changing the demonstration and templates.</li> <li>ICL is similar to the decision process of human beings by learning from analogy</li> <li>ICL is training free.</li> </ol> <h1 id="definition">Definition</h1> <p>In-context learning is a paradigm that allows language models to learn tasks given only a few examples in the form of demonstration.</p> <p>Formally, given a query $x$ and a set of candidate answers $Y={y_1, \ldots, y_m}$ (here $Y$ can be class labels, or a set of free-text phrases), a LLM $\mathcal{M}$ takes the candidate answer with the max score as the prediction, conditioned a demonstration set $C$.</p> <p>Here $C$ can be ${I, s(x_1, y_1), \ldots, s(x_k, y_k)}$ where $s(x_i, y_i)$ is an example pair, $I$ is an instruction (optional), and these $y_i$ in $C$ are different from $Y$.</p> <p>Here depending on whether examples in $C$ belong to the same task or not, ICL can be classified as task-specific ICL and cross-task ICL</p> <h3 id="some-terminology-clarification">Some terminology clarification</h3> <p>Few shot learning: a general ML approach that involves adapting model parameters to perform a task with a limited number of supervised examples.</p> <p>ICL does not train the model, hence no weights are modified.</p> <h1 id="arena-of-icl">Arena of ICL</h1> <h2 id="how-did-the-icl-capability-happen">How did the ICL capability happen</h2> <ol> <li>pretraining or continual pretraining <ol> <li>aggregating related contexts, making the models learn to reason across prior demonstrations.</li> </ol> </li> <li>Warmup <ol> <li>this is a way to enhance ICL ability</li> <li>adding a continual training stage between pretraining and ICL inference.</li> <li>this modify or add LLM parameters.</li> <li>most of the works are in 2022-2023.</li> </ol> </li> </ol> <h2 id="prompt-designing">Prompt designing</h2> <p>Major usage of ICL is during inference. This section is about how to boost the performance of ICL by prompt-engineering.</p> <h3 id="demonstration-organization">Demonstration organization</h3> <ol> <li>Selection: which are the good samples? <ol> <li>One un-surpervised option is: choose the nearest neighbors of input instances based on their similarities</li> <li>supervised way: train a dense retriever for demonstration selection (use human-labeled or LLM-labeled demonstrations)</li> <li>RL way: demonstration selection can be a Markov decision process; the action is choosing example; reward is defined as the accuracy of a labeled validation set; selected demonstration can be learned via Q-learning.</li> </ol> </li> <li>Formatting: here main stream is to use LLM to reformat the demonstrations</li> <li>Ordering <ol> <li>arrange examples based on their proximity to the input.</li> <li>use global and local entropy metrics, finding a positive correlation between these metrics and the ICL performance</li> <li>or gradually increase the complexity of the demonstrations (simple → difficult)</li> </ol> </li> </ol> <h1 id="why-icl-works">Why ICL works?</h1> <h2 id="influencing-factors">Influencing factors</h2> <ol> <li> <p>Pretraining stage</p> <ol> <li>combining multiple source domain is more important to lead to the emergence of ICL (than corpus size)</li> <li>task diversity</li> <li>training data with certain distributional properties (bustiness, item appear in clusters, rather than being uniformly distributed over time)</li> <li>This is an interesting found: during the inference, ICL samples should attend to each other (current causal LLM may lead to suboptimal ICL performance)</li> </ol> </li> <li> <p>Inference Stage</p> <p>There are some tricks here to order the difficulty of sample, mixture positive/negative pairs, etc can affect the performance of ICL.</p> </li> </ol> <h2 id="learning-mechanisms-of-icl">Learning mechanisms of ICL</h2> <p>Here the author list some of the research studying the reason of ICL capability for LLM. From the transformer architecture perspective, it can identify that some specific attention head (”induction head”) can replicate previous patterns for next-token gen. By studying the info-flow in transformer, ICT shows that some label words serve as (attention) anchors, which aggregate and distribute key info for the final pred.</p> <p>There are also some work focus on bayesian and gradient descent methods.</p> <h1 id="appendix">Appendix</h1> <h3 id="causallm-is-not-optimal-for-in-context-learning">CAUSALLM IS NOT OPTIMAL FOR IN-CONTEXT LEARNING</h3> <p>This paper is from Google Research.</p> <p>This paper provided a simplified version of proof for the empirical evidence that ICL performs better with prefixLM than causalLM.</p> <p><strong>Two attention formulas</strong></p> <p>Here we clarify the two attention formulas, namely prefixLM and causalLM. We start with general form of attention layer</p> \[\begin{aligned} z_j &amp;\leftarrow z_j + \mathbf{PVZ} \mathtt{softmax} (\mathbf{Z}^T \mathbf{K}^T \mathbf{Q} z_j) \\ &amp;= z_j + \mathbf{PV} \sum_{i=1}^n z_i\mathtt{softmax}_i (\mathbf{Z}^T \mathbf{K}^T \mathbf{Q} z_j) \end{aligned}\] <p>where $\mathbf{Z} = (z_1, \ldots, z_n)$ is the (embedded) input tokens, and $\mathbf{P}, \mathbf{V}, \mathbf{K}, \mathbf{Q}$ corresponds to output projection, value, key, query projections, respectively.</p> <p>Attention in this format is called full attention, namely each input token (embedded) $z_j$ can attend to all positions $i\in{1, \ldots, n}.$ This type of attention is mainly used in encoder-based models, like BERT, embedding models etc.</p> <p>For decoder-only model, it is common to use auto-regressive or causal attention, namely</p> \[z_j \leftarrow z_j + \mathbf{PV} \sum_{i=1}^j z_i\mathtt{softmax}_i (\mathbf{Z}_{[1,\ldots,j]}^T \mathbf{K}^T \mathbf{Q} z_j)\] <p>In this format, each token $z_j$ is restricted to attend only to previous token and itself.</p> <p>Another version of attention is based on the observation that some tasks can benefit from a prefix sequence such as context or prompt. In this case, the input sequence $\mathbf{Z} = (z_1, \ldots, z_{n’}, z_{n’+1}, \ldots, z_{n})$ where $[1, \ldots, n’]$ are prefix tokens, and the remaining are sample (in ICL sense). Then prefixLM suggests the following attention formula</p> \[z_j \leftarrow z_j + \mathbf{PV} \sum_{i=1}^{\max (j, n')} z_i\mathtt{softmax}_i (\mathbf{Z}_{[1,\ldots,\max (j, n')]}^T \mathbf{K}^T \mathbf{Q} z_j)\] <p>The main conclusion of this paper is that the linearized version of prefixLM is optimal comparing to (linearized) causalLM. But the proof is only under some quite strong assumption (like LSA in the shape of (5) ), so even though the prefixLM sounds more reasonable in ICL, it is really hard to prove in the term of general attention.</p> <p>Furthermore, the modern CoT makes the causalLM more powerful, hence harder to prove. But this method holds as a valid research direction, especially with more recent LLM like Qwen3 (SFT a ICT version of it with a mask as in prefixLM).</p>]]></content><author><name></name></author><category term="paper-reading"/><category term="in-context-learning"/><category term="icl"/><category term="llm"/><category term="prompt-engineering"/><category term="few-shot-learning"/><summary type="html"><![CDATA[Survey notes on in-context learning (ICL) - a paradigm for LLMs to learn tasks from few examples without parameter updates. Covers ICL mechanisms, prompt engineering, and comparison of causalLM vs prefixLM.]]></summary></entry><entry><title type="html">Transformer Basics: Architecture and Data Flow</title><link href="https://jq-wei.github.io/blog/2025/transformer-basics/" rel="alternate" type="text/html" title="Transformer Basics: Architecture and Data Flow"/><published>2025-12-13T00:00:00+00:00</published><updated>2025-12-13T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2025/transformer-basics</id><content type="html" xml:base="https://jq-wei.github.io/blog/2025/transformer-basics/"><![CDATA[<p>Here we review the data flow of transformer architecture. This is a 2-D problem: one from the perspective of model layers; one from the sequence of the tokens.</p> <h1 id="model-architecture">Model architecture</h1> <p>In this part, we only look at a simplified version of transformer, namely decoder-only transformer.</p> <p><img src="/assets/img/transformer-basics/Screenshot_2025-12-08_at_23.12.31.png" width="60%" alt="Screenshot 2025-12-08 at 23.12.31.png"/></p> <h2 id="token-embedding">Token embedding</h2> <p>$x_0 = W_E t$</p> <p>where $t\in \mathbb{R}^{n_{context}\times n_{vocab} }$ is input tokens (one-hot encoded tokens), $W_E\in\mathbb{R}^{d_{model}\times n_{vocab}}$ is the embedding parameter.</p> <p>Hence $x_0\in\mathbb{R}^{d_{model}\times n_{context}}$</p> <p>Together ROPE we have this part written in this code format:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># class Qwen3Model forward
</span>				<span class="k">if</span> <span class="n">inputs_embeds</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">inputs_embeds</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">embed_tokens</span><span class="p">(</span><span class="n">input_ids</span><span class="p">)</span>

        <span class="bp">...</span>

        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">inputs_embeds</span>

        <span class="c1"># create position embeddings to be shared across the decoder layers
</span>        <span class="n">position_embeddings</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">rotary_emb</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">,</span> <span class="n">position_ids</span><span class="p">)</span>

        <span class="k">for</span> <span class="n">decoder_layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[:</span> <span class="n">self</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">num_hidden_layers</span><span class="p">]:</span>
            <span class="n">hidden_states</span> <span class="o">=</span> <span class="nf">decoder_layer</span><span class="p">()</span>
</code></pre></div></div> <h2 id="decoderlayer">DecoderLayer</h2> <p>For this part we only focus the multi-head attention and MLP part after it, and skip layer_norm since it does not change the tensor shape, but just normalize it.</p> <p>In mathematic term, this part essentially does</p> <ul> <li>Each attention head $h$, the latent state updates according to $x_{i+1} = x_i + \sum_{h\in H_i} h(x_i)$</li> </ul> <p>where $H_i$ is the set of attention heads at layer $i$.</p> <ul> <li>MLP is run and add the residual: $x_{i+2} = x_{i+1} + m(x_{i+1})$</li> </ul> <p>Here we use the index $i$ to indicate this is calculation on layer $i$. The full picture is simplified as</p> <p><img src="/assets/img/transformer-basics/Screenshot_2025-12-09_at_10.49.44.png" width="60%" alt="Screenshot 2025-12-09 at 10.49.44.png"/></p> <p>Here $x_i, x_{i+1}, x_{i+2} \in\mathbb{R}^{n_{context}\times d_{model}}$</p> <p>Next, let us move on the attention calculation part.</p> <h3 id="attention">Attention</h3> <p>The attention (output) is given by</p> \[\mathtt{Attention}(Q,K,V) = \mathtt{softmax}(\frac{QK^T}{\sqrt{d_k}})V\] <p>Usually the softmax part is referred as attention_weight. $\sqrt{d_k}$ is the scaling factor.</p> <p>The calculation of attention weight can be equivalently expressed in the following equation (less efficient thought)</p> \[\mathtt{attnWeight} = \mathtt{softmax}(x^TW_{Q}^T W_K x)\] <p>where $W_Q, W_K, W_V \in \mathbb{R}^{n_{head}*d_{head} \times d_{model}}$ (before GQA).</p> <p>After the calculation of the Attention, there is a projection process to make the $\mathtt{Attention}(Q,K,V)$ to the shape of the $n_{context}\times d_{model}$, so we continue the MLP and residual part of the decoderLayer. Then iterate till the final layer.</p> <h3 id="towards-logits">Towards Logits</h3> <p>After the last layer of the decoder, some calculations are needed to derive the final probability distribution of the next token (this part can be different for training/sft).</p> <p>For inference only (no matter prefill or decoding), we only need the last or latest token hidden_state to predict the next token. In this case we do a slice of the final hidden_states (<code class="language-plaintext highlighter-rouge">logits_to_keep = 1</code>).</p> <p>In equations,</p> <ul> <li>$x_N\in\mathbb{R}^{n_{context}\times d_{model}}$ → $x_{Ns}\in\mathbb{R}^{1\times d_{model}}$</li> <li>$\mathtt{logits} = W_u x_{Ns} \in\mathbb{R}^{1\times n_{vocab}}$</li> </ul> <p>where $W_N \in\mathbb{R}^{n_{vocab}\times d_{model}}.$</p> <p>Then the logits will be normalized as a probability distribution to predict the next token. There are many protocols to play with and accelerate.</p> <h1 id="prefill-vs-decode">Prefill vs Decode</h1> <p>In the previous sections, we explore and trace through the forward path of the transformer architecture. This can be y-axis of the problem. Then x-axis is from the sequence perspective.</p> <p>We start with 12 tokens (<code class="language-plaintext highlighter-rouge">&lt;|im_start|&gt;user\nHow are you?&lt;|im_end|&gt;\n&lt;|im_start|&gt;assistant\n</code>) , and the model generates <code class="language-plaintext highlighter-rouge">'I'm functioning well, thank you for asking!'</code> (9 new tokens!)</p> <ul> <li>At the prefill, we run the forward path of the model with 12 tokens, layer by layer <ul> <li>this path can be compute bounded (many matrices multiplications)</li> <li><code class="language-plaintext highlighter-rouge">seq_len = hidden_states.shape[1] = key_states.shape[2] &gt;1</code></li> <li>For Qwen3, <code class="language-plaintext highlighter-rouge">past_key_values</code> is of class <code class="language-plaintext highlighter-rouge">DynamicCache</code></li> <li>before we enter a decoderLayer, the <code class="language-plaintext highlighter-rouge">past_key_values</code> of that layer is not None, but empty, i.e.,</li> </ul> </li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># we only check 0-th layer.
</span>
<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
  <span class="n">seq_len</span> <span class="o">=</span> <span class="n">query_states</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
  <span class="n">stage</span> <span class="o">=</span> <span class="sh">"</span><span class="s">PREFILL</span><span class="sh">"</span> <span class="k">if</span> <span class="n">seq_len</span> <span class="o">&gt;</span> <span class="mi">1</span> <span class="k">else</span> <span class="sh">"</span><span class="s">DECODE</span><span class="sh">"</span>

  <span class="k">if</span> <span class="n">past_key_values</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
      <span class="c1"># Check cache length BEFORE update
</span>      <span class="n">cache_len_before</span> <span class="o">=</span> <span class="n">past_key_values</span><span class="p">.</span><span class="nf">get_seq_length</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span><span class="p">)</span>  <span class="c1"># 0
</span>

<span class="k">if</span> <span class="n">past_key_values</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
  <span class="c1"># sin and cos are specific to RoPE models; cache_position needed for the static cache
</span>  <span class="n">cache_kwargs</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">sin</span><span class="sh">"</span><span class="p">:</span> <span class="n">sin</span><span class="p">,</span> <span class="sh">"</span><span class="s">cos</span><span class="sh">"</span><span class="p">:</span> <span class="n">cos</span><span class="p">,</span> <span class="sh">"</span><span class="s">cache_position</span><span class="sh">"</span><span class="p">:</span> <span class="n">cache_position</span><span class="p">}</span>
  <span class="n">key_states</span><span class="p">,</span> <span class="n">value_states</span> <span class="o">=</span> <span class="n">past_key_values</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">key_states</span><span class="p">,</span> <span class="n">value_states</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span><span class="p">,</span> <span class="n">cache_kwargs</span><span class="p">)</span>

  <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
      <span class="n">cache_len_after</span> <span class="o">=</span> <span class="n">past_key_values</span><span class="p">.</span><span class="nf">get_seq_length</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">layer_idx</span><span class="p">)</span> <span class="c1"># 12
</span></code></pre></div></div> <p>This can be imagined as drawer opening for a closet, if the drawer is empty, put the KV there and move on to above layer.</p> <ul> <li>At the decode, the newly generated token from the last step will go through the transformers <ul> <li>All the steps before attention calculation can be done by itself (embedding, projection to Q K V etc)</li> <li>Attention is done between this new token to the previous existing tokens.</li> <li>Before we enter a layer, the <code class="language-plaintext highlighter-rouge">past_key_values</code> of that layer is none empty. For example, we generate one new token after prefill, and now we are at layer 0. The <code class="language-plaintext highlighter-rouge">past_key_values</code> of this layer has 12 (13) key and values before (after) <code class="language-plaintext highlighter-rouge">past_key_values.update(key_states, value_states, self.layer_idx, cache_kwargs)</code></li> <li>This basically expand the lower-triangle attention matrix (<code class="language-plaintext highlighter-rouge">attn_weights</code>) by one more row.</li> </ul> </li> </ul> <h1 id="qwen3-analogies">Qwen3 analogies</h1> <p>In this part, we verify the tensor-flow using a minimal inference logic:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoModelForCausalLM</span><span class="p">,</span> <span class="n">AutoTokenizer</span>

<span class="c1"># Model path - can be HuggingFace ID or local path
</span><span class="n">MODEL_ID</span> <span class="o">=</span> <span class="sh">"</span><span class="s">local/path/to/Qwen3-4B-Instruct-2507</span><span class="sh">"</span>

<span class="c1"># Device selection for Mac M4
</span><span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="n">backends</span><span class="p">.</span><span class="n">mps</span><span class="p">.</span><span class="nf">is_available</span><span class="p">():</span>
    <span class="n">DEVICE</span> <span class="o">=</span> <span class="sh">"</span><span class="s">mps</span><span class="sh">"</span>  <span class="c1"># Apple Silicon GPU
</span><span class="k">elif</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">():</span>
    <span class="n">DEVICE</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span>
<span class="k">else</span><span class="p">:</span>
    <span class="n">DEVICE</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span>

<span class="c1"># =============================================================================
# Load Model and Tokenizer
# =============================================================================
</span>
<span class="n">tokenizer</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">MODEL_ID</span><span class="p">,</span>
    <span class="n">trust_remote_code</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">MODEL_ID</span><span class="p">,</span>
    <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span>  <span class="c1"># torch.bfloat16
</span>    <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">,</span>          <span class="c1"># Auto device placement
</span>    <span class="n">trust_remote_code</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># =============================================================================
# Inference Function
# =============================================================================
</span>
<span class="k">def</span> <span class="nf">generate</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">10</span><span class="p">,</span> <span class="n">enable_thinking</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Generate a response from the model.

    Args:
        prompt: The user</span><span class="sh">'</span><span class="s">s input
        max_new_tokens: Max tokens to generate
        enable_thinking: If True, model shows reasoning process (default: False)
    </span><span class="sh">"""</span>

    <span class="c1"># For Qwen3 Thinking models, add /no_think to disable reasoning output
</span>
    <span class="c1"># Format as chat (Qwen uses ChatML format)
</span>    <span class="n">messages</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">prompt</span><span class="p">}</span>
    <span class="p">]</span>

    <span class="c1"># Apply chat template
</span>    <span class="n">text</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span>
        <span class="n">messages</span><span class="p">,</span>
        <span class="n">tokenize</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
        <span class="n">add_generation_prompt</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Formatted prompt:</span><span class="se">\n</span><span class="si">{</span><span class="nf">repr</span><span class="p">(</span><span class="n">text</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Tokenize
</span>    <span class="n">inputs</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>

    <span class="c1"># Generate
</span>    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">outputs</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
            <span class="o">**</span><span class="n">inputs</span><span class="p">,</span>
            <span class="n">max_new_tokens</span><span class="o">=</span><span class="n">max_new_tokens</span><span class="p">,</span>
            <span class="n">do_sample</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
            <span class="n">temperature</span><span class="o">=</span><span class="mf">0.7</span><span class="p">,</span>
            <span class="n">top_p</span><span class="o">=</span><span class="mf">0.8</span><span class="p">,</span>
            <span class="n">top_k</span><span class="o">=</span><span class="mi">20</span><span class="p">,</span>
            <span class="n">pad_token_id</span><span class="o">=</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">eos_token_id</span><span class="p">,</span>
        <span class="p">)</span>

    <span class="c1"># Decode (skip input tokens)
</span>    <span class="n">response</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span>
        <span class="n">outputs</span><span class="p">[</span><span class="mi">0</span><span class="p">][</span><span class="n">inputs</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">].</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]:],</span>
        <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>

<span class="c1"># =============================================================================
# Main
# =============================================================================
</span>
<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># Test prompt
</span>    <span class="n">prompt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">How are you?</span><span class="sh">"</span>

    <span class="n">response</span> <span class="o">=</span> <span class="nf">generate</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>

    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">💬 Response:</span><span class="se">\n</span><span class="si">{</span><span class="n">response</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>The testing prompt is formatted as <code class="language-plaintext highlighter-rouge">'&lt;|im_start|&gt;user\nHow are you?&lt;|im_end|&gt;\n&lt;|im_start|&gt;assistant\n'</code> which has 12 tokens.</p> <p>Some of the key parameters are as follows:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="sh">"</span><span class="s">head_dim</span><span class="sh">"</span><span class="p">:</span> <span class="mi">128</span><span class="p">,</span>
<span class="sh">"</span><span class="s">hidden_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">2560</span><span class="p">,</span>
<span class="sh">"</span><span class="s">intermediate_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">9728</span><span class="p">,</span>
<span class="sh">"</span><span class="s">max_window_layers</span><span class="sh">"</span><span class="p">:</span> <span class="mi">36</span><span class="p">,</span>
<span class="sh">"</span><span class="s">num_attention_heads</span><span class="sh">"</span><span class="p">:</span> <span class="mi">32</span><span class="p">,</span>
<span class="sh">"</span><span class="s">num_hidden_layers</span><span class="sh">"</span><span class="p">:</span> <span class="mi">36</span><span class="p">,</span>
<span class="sh">"</span><span class="s">num_key_value_heads</span><span class="sh">"</span><span class="p">:</span> <span class="mi">8</span><span class="p">,</span>
<span class="sh">"</span><span class="s">vocab_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">151936</span>
</code></pre></div></div> <p>Some reference to our notation later</p> <ul> <li>$d_{model} = 2560 (\mathtt{hidden}_ \mathtt{size})$</li> <li>$n_{vocab} = 151936$</li> </ul> <p>We first focus on the prefilling stage.</p> <h3 id="token-embedding-1">Token embedding</h3> <p>For our testing prompt, the <code class="language-plaintext highlighter-rouge">input_ids</code> $t$ is of shape <code class="language-plaintext highlighter-rouge">torch.Size([1, 12])</code> , after embedding the tensor $x_0$ is the <code class="language-plaintext highlighter-rouge">hidden_states</code> to the first layer of the decoderLayer with shape <code class="language-plaintext highlighter-rouge">torch.Size([1, 12, 2560])</code> .</p> <p>The shape of the embedding weights is <code class="language-plaintext highlighter-rouge">torch.Size([151936, 2560])</code> .</p> <h3 id="decoderlayer-part1">DecoderLayer-part1</h3> <p>For this part we leave the attention part as a black box for now, and only track the tensor shape of the hidden_states in/out decoderLayer.</p> <p>This is still for prefill.</p> <p>The shape is identical to all the layers.</p> <ul> <li>Input tensor: <code class="language-plaintext highlighter-rouge">hidden_states</code> is of shape <code class="language-plaintext highlighter-rouge">torch.Size([1, 12, 2560])</code></li> <li>After Attention + Residual: the shape is unchanged as <code class="language-plaintext highlighter-rouge">torch.Size([1, 12, 2560])</code></li> <li>After MLP + Residual: still <code class="language-plaintext highlighter-rouge">torch.Size([1, 12, 2560])</code></li> </ul> <p>Note here <code class="language-plaintext highlighter-rouge">1</code> is batch_size.</p> <p>In code this part is essentially as follows</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># class Qwen3DecoderLayer forward
</span>				<span class="n">residual</span> <span class="o">=</span> <span class="n">hidden_states</span>
        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">input_layernorm</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
        <span class="c1"># Self Attention
</span>        <span class="n">hidden_states</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn</span><span class="p">(...)</span>
        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">residual</span> <span class="o">+</span> <span class="n">hidden_states</span>

        <span class="c1"># Fully Connected
</span>        <span class="n">residual</span> <span class="o">=</span> <span class="n">hidden_states</span>
        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">post_attention_layernorm</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">mlp</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
        <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">residual</span> <span class="o">+</span> <span class="n">hidden_states</span>
</code></pre></div></div> <h3 id="attention-calculation-part-1">Attention calculation: part 1</h3> <p>In the real implementation, the big matrix $W^T_QW_K$ is never computed. Instead the tensor $Q,K,V$ are computed</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">q_norm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">q_proj</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="n">hidden_shape</span><span class="p">)).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="n">key_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">k_norm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">k_proj</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="n">hidden_shape</span><span class="p">)).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="n">value_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">v_proj</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="n">hidden_shape</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <p>where the starting <code class="language-plaintext highlighter-rouge">hidden_states</code> is of shape <code class="language-plaintext highlighter-rouge">[1, 12, 2560]</code> and $W_Q, W_K, W_V$ are of shape</p> <p><code class="language-plaintext highlighter-rouge">[4096, 2560], [1024, 2560], [1024, 2560]</code> respectively. In this case, qwen3 use GQA namely every 4 queries share one pair of KV. This saves the KV cache largely.</p> <p>After the projection, query state ($Q$), key state ($K$), value state ($V$) become</p> <p><code class="language-plaintext highlighter-rouge">[1, 12, 4096], [1, 12, 1024], [1, 12, 1024]</code> . By applying the view, they turn into</p> <p><code class="language-plaintext highlighter-rouge">[1, 12, 32, 128], [1, 12, 8, 128], [1, 12, 8, 128]</code> . Eager and sdpa implementation of attention has the repeat_kv function which duplicates the K V to match Q.</p> <p>The final transpose(1,2) is to make all tensors into shape <code class="language-plaintext highlighter-rouge">[batch_size, n_heads, seq_len, head_dim]</code> so that we can calculate the <code class="language-plaintext highlighter-rouge">attn_weights</code></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">attn_weights</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">key_states</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">))</span> <span class="o">*</span> <span class="n">scaling</span>
</code></pre></div></div> <p>where attn_weights is of shape <code class="language-plaintext highlighter-rouge">[batch_size, n_heads, seq_len, seq_len]</code> . This weights matrix is showing: for each head, how much each query token attends to each key tokens. For causal case, the following example show the first head attention matrix</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">attn_weights</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="p">:,</span> <span class="p">:]</span> <span class="o">=</span>   <span class="c1"># Head 0's attention matrix
</span>       <span class="n">K</span><span class="err">₀</span>    <span class="n">K</span><span class="err">₁</span>    <span class="n">K</span><span class="err">₂</span>    <span class="n">K</span><span class="err">₃</span>   <span class="p">...</span>  <span class="n">K</span><span class="err">₁₁</span>
<span class="n">Q</span><span class="err">₀</span>   <span class="p">[</span><span class="mf">0.9</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span> <span class="p">...,</span> <span class="mf">0.0</span><span class="p">]</span>   <span class="c1"># Q₀ only sees K₀ (causal)
</span><span class="n">Q</span><span class="err">₁</span>   <span class="p">[</span><span class="mf">0.3</span><span class="p">,</span>  <span class="mf">0.7</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span> <span class="p">...,</span> <span class="mf">0.0</span><span class="p">]</span>   <span class="c1"># Q₁ sees K₀, K₁
</span><span class="n">Q</span><span class="err">₂</span>   <span class="p">[</span><span class="mf">0.1</span><span class="p">,</span>  <span class="mf">0.2</span><span class="p">,</span>  <span class="mf">0.7</span><span class="p">,</span>  <span class="mf">0.0</span><span class="p">,</span> <span class="p">...,</span> <span class="mf">0.0</span><span class="p">]</span>   <span class="c1"># Q₂ sees K₀, K₁, K₂
</span><span class="bp">...</span>
<span class="n">Q</span><span class="err">₁₁</span>  <span class="p">[</span><span class="mf">0.05</span><span class="p">,</span> <span class="mf">0.05</span><span class="p">,</span> <span class="mf">0.1</span><span class="p">,</span>  <span class="mf">0.1</span><span class="p">,</span> <span class="p">...,</span> <span class="mf">0.5</span><span class="p">]</span>   <span class="c1"># Q₁₁ sees all K₀-K₁₁
</span></code></pre></div></div> <p>Note this is before softmax so we can have the first row with 0.9.</p> <p>The following code is to add the mask (which makes the unseen part to be -inf for instance), then softmax to normalize it.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">if</span> <span class="n">attention_mask</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">causal_mask</span> <span class="o">=</span> <span class="n">attention_mask</span><span class="p">[:,</span> <span class="p">:,</span> <span class="p">:,</span> <span class="p">:</span> <span class="n">key_states</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">2</span><span class="p">]]</span>
        <span class="n">attn_weights</span> <span class="o">=</span> <span class="n">attn_weights</span> <span class="o">+</span> <span class="n">causal_mask</span>

    <span class="n">attn_weights</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="n">functional</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">attn_weights</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float32</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">query</span><span class="p">.</span><span class="n">dtype</span><span class="p">)</span>
</code></pre></div></div> <p>So far, we have the attention matrix (prefill stage) with shape <code class="language-plaintext highlighter-rouge">[1, 32, 12, 12]</code> , then the final $\mathtt{Attention}$ is given by $\mathtt{Attn}_\mathtt{weights} * V$ which is <code class="language-plaintext highlighter-rouge">[1, 32, 12, 128]</code> .</p> <p>With a transpose, the final output of the attention function is <code class="language-plaintext highlighter-rouge">attn_output, attn_weights</code> from <code class="language-plaintext highlighter-rouge">[1, 12, 32, 128], [1, 32, 12, 12]</code></p> <p>Final note about scaling factor def:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">scaling</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">head_dim</span><span class="o">**-</span><span class="mf">0.5</span> <span class="c1"># head_dim = 128
</span></code></pre></div></div> <h3 id="attention-calculation-part-2">Attention calculation: part 2</h3> <p>After calculation of attention, and before we proceed to the MLP part of DecoderLayer, we need to one more projection.</p> <p>Recall that the final output from the previous attention calculation <code class="language-plaintext highlighter-rouge">attn_output</code> is of shape <code class="language-plaintext highlighter-rouge">[1, 12, 32, 128]</code> (i.e., <code class="language-plaintext highlighter-rouge">[batch_size, seq_len, n_heads, head_dim]</code>).</p> <ul> <li>First reshape the <code class="language-plaintext highlighter-rouge">attn_output</code> to <code class="language-plaintext highlighter-rouge">[batch_size, seq_len, n_heads*head_dim]</code> , i.e., <code class="language-plaintext highlighter-rouge">[1, 12, 4096]</code></li> <li>Project with weights in shape <code class="language-plaintext highlighter-rouge">[2560, 4096]</code></li> <li>The final output of the attention block (then we return to the decoderLayer remaining part) is of shape <code class="language-plaintext highlighter-rouge">[1, 12, 2560]</code></li> </ul> <p>This part in code is</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">attn_output</span> <span class="o">=</span> <span class="n">attn_output</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">*</span><span class="n">input_shape</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">contiguous</span><span class="p">()</span>
<span class="n">attn_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">o_proj</span><span class="p">(</span><span class="n">attn_output</span><span class="p">)</span>
</code></pre></div></div> <h3 id="decoderlayer-part2">DecoderLayer-part2</h3> <p>This section is for the remaining part of the decoderLayer: residual, and MLP.</p> <p>Note that the final output from attention calculation is of the same shape as the input <code class="language-plaintext highlighter-rouge">hidden_states</code> to decoderLayer, i.e., <code class="language-plaintext highlighter-rouge">[1, 12, 2560]</code> (<code class="language-plaintext highlighter-rouge">[batch_size, seq_len, d_model]</code>). For both residual and MLP part, the <code class="language-plaintext highlighter-rouge">hidden_state</code> will keep this shape, and output.</p> <p>The input of the previous decoderLayer will be the input to the next one until final one.</p> <p>The residual + MLP in code is</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">residual</span> <span class="o">=</span> <span class="n">hidden_states</span>

<span class="p">....</span><span class="c1"># attention cal
</span>
<span class="n">hidden_states</span> <span class="o">=</span> <span class="n">residual</span> <span class="o">+</span> <span class="n">hidden_states</span>

<span class="c1"># Fully Connected
</span><span class="n">residual</span> <span class="o">=</span> <span class="n">hidden_states</span>
<span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">post_attention_layernorm</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
<span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">mlp</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
<span class="n">hidden_states</span> <span class="o">=</span> <span class="n">residual</span> <span class="o">+</span> <span class="n">hidden_states</span>
</code></pre></div></div> <h3 id="towards-logits-1">Towards Logits</h3> <p>This part is from the output of the last decoderLayer to the logits. The equation in code is given as</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">hidden_states</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">last_hidden_state</span>

<span class="c1"># Only compute necessary logits, and do not upcast them to float if we are not computing the loss
</span><span class="n">slice_indices</span> <span class="o">=</span> <span class="nf">slice</span><span class="p">(</span><span class="o">-</span><span class="n">logits_to_keep</span><span class="p">,</span> <span class="bp">None</span><span class="p">)</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">logits_to_keep</span><span class="p">,</span> <span class="nb">int</span><span class="p">)</span> <span class="k">else</span> <span class="n">logits_to_keep</span>

<span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">[:,</span> <span class="n">slice_indices</span><span class="p">,</span> <span class="p">:])</span>
</code></pre></div></div> <p>Here the <code class="language-plaintext highlighter-rouge">hidden_states</code> is the last layer’s output with shape <code class="language-plaintext highlighter-rouge">[1, 12, 2560]</code> (<code class="language-plaintext highlighter-rouge">[batch_size, seq_len, d_model]</code>).</p> <ul> <li>For both prefill and decode, we only care about the last tokens <code class="language-plaintext highlighter-rouge">hidden_states</code> , so there is a slice with <code class="language-plaintext highlighter-rouge">logits_to_keep = 1</code> .</li> <li>The sliced <code class="language-plaintext highlighter-rouge">hidden_state</code> is of shape <code class="language-plaintext highlighter-rouge">[1,1,2560]</code></li> <li><code class="language-plaintext highlighter-rouge">lm_head.weight</code> is of shape <code class="language-plaintext highlighter-rouge">[151936, 2560]</code> (<code class="language-plaintext highlighter-rouge">[n_vocab, d_model]</code>)</li> <li>Hence the final <code class="language-plaintext highlighter-rouge">logits</code> is <code class="language-plaintext highlighter-rouge">[1, 1, 151936]</code> (the first 1 is batch_size)</li> </ul> <p>Note that even though we only care about the last or the latest token’s hidden state, we still need to do the whole prefill because:</p> <ol> <li>the last or the latest (prefill or decode) tokens info can not be known without calculating the previous tokens’ info</li> <li>KV cache, so later coming tokens don’t have to recompute the previous tokens key and value to obtain its attention weights</li> </ol> <h1 id="ref">Ref</h1> <p>https://transformer-circuits.pub/2021/framework/index.html</p> <p>https://arxiv.org/pdf/1706.03762 (attention is all you need)</p> <p>https://arxiv.org/pdf/2305.07716 (decoder only transformer)</p>]]></content><author><name></name></author><category term="technical-notes"/><category term="transformer"/><category term="llm"/><category term="architecture"/><category term="attention"/><category term="deep-learning"/><summary type="html"><![CDATA[Understanding the transformer architecture from model layers and token sequence perspectives]]></summary></entry><entry><title type="html">HippoRAG: Neurobiologically Inspired Long-Term Memory for LLMs</title><link href="https://jq-wei.github.io/blog/2025/hipporag-paper-notes/" rel="alternate" type="text/html" title="HippoRAG: Neurobiologically Inspired Long-Term Memory for LLMs"/><published>2025-11-23T00:00:00+00:00</published><updated>2025-11-23T00:00:00+00:00</updated><id>https://jq-wei.github.io/blog/2025/hipporag-paper-notes</id><content type="html" xml:base="https://jq-wei.github.io/blog/2025/hipporag-paper-notes/"><![CDATA[<h1 id="hipporag-neurobiologically-inspired-long-term-memory-for-llms">HippoRAG: Neurobiologically Inspired Long-Term Memory for LLMs</h1> <p>This paper propose an new RAG paradigm using knowledge graph, which is inspired by neural science and hippocampus.</p> <h1 id="main-goal">Main goal</h1> <p>The main goal is still to get the most relevant passages (paragraphs) given user query.</p> <p>Still a traditional RAG.</p> <p>It is NOT: trying to answer queries directly from RAG</p> <p>It is NOT: returning entities to the LLM</p> <p>In short, this paper use PageRank algorithm to find the optimal path/tree for entities and return the chunks containing most of those entities.</p> <h1 id="main-architecture">Main architecture</h1> <p>The HippoRAG method takes two main steps: Offline Indexing, and Online Retrieval</p> <h2 id="some-notations">Some notations</h2> <p>$P$ : number of passages</p> <p>$L$: LLM</p> <p>$M:$ retrieval Encoder</p> <p>$N:$ set of noun nodes (entities)</p> <p>$E:$ relation edges</p> <h2 id="offline-indexing">Offline Indexing</h2> <p><strong>For each passage P:</strong></p> <p>Step 1: use L to extract N, the prompt is as the follows:</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_13.17.19.png" width="80%" alt="Screenshot 2025-11-18 at 13.17.19.png"/></p> <p>Step 2: and use OpenIE (an NLP LLM) to extract the triplet. This will create relation edges E among all the entities (within this passage). This relation is of type <strong>Fact</strong>.</p> <p>The prompt used in this step is</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_13.17.58.png" alt="Screenshot 2025-11-18 at 13.17.58.png"/></p> <p><strong>After done step 1-2 for each passages</strong></p> <p>Step 3. Now we aggregate all the unique entities and facts across all passages. This will give us a global Knowledge Graph (KG).</p> <p>Step 4. Additionally, we can use a encoder model M to add synonymy relations if there similarity is above a threshold $\tau$.</p> <p>One example is:</p> <p>Given two passage as the following:</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_13.32.32.png" alt="Screenshot 2025-11-18 at 13.32.32.png"/></p> <p>Then NER and OpenIE triplet of them are:</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_13.33.24.png" alt="Screenshot 2025-11-18 at 13.33.24.png"/></p> <p>And the final graph (of these two passages) is given as:</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_13.34.28.png" alt="Screenshot 2025-11-18 at 13.34.28.png"/></p> <p>Note that there are:</p> <ul> <li>3-types of edges: <ol> <li>Fact-edges: entity ↔ entity</li> <li>Passage-edges: passage ↔ entity (connect each passage, to ALL entities that appear in its triplets)</li> <li>Synonymy-edges: entity ↔ similar entity</li> </ol> </li> <li>2-types of nodes <ol> <li>entity-node</li> <li>passage-node: in this case, passages with shared entities are ‘connected’.</li> </ol> </li> </ul> <p>After this process, we have the following matrix</p> \[\mathbf{P} \in \mathbb{R}^{|N|\times|P|}\] <p>where each row corresponds to each entity and how many times it appears in each passages (columns).</p> <h2 id="online-retrieval">Online Retrieval</h2> <p><strong>Step 1.</strong> Given a query $q$, use the same prompt + LLM to extract named entities</p> \[C_q = \{c_1, \ldots, c_n \}.\] <p>The named entities $C_q$ is then mapped to $N$ (the set of entities from KG) using the max cos_sim, i.e., we got the so-called query nodes defined as</p> \[R_q = \{ r_1, \ldots, r_n \}\] <p>where $r_i$ is the closest entity to $c_i$ from $N$, namely</p> \[r_i = \arg\max_{e_j\in N} cossim (M(c_i), M(e_j))\] <p>where M is the same encoder model.</p> <p><strong>One risk:</strong> there is a potential risk that some of the entity from the query are not properly matched, or even missed! For example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query</span><span class="p">:</span> <span class="sh">"</span><span class="s">What county is Erik Hort</span><span class="sh">'</span><span class="s">s birthplace in?</span><span class="sh">"</span>
<span class="n">C_q</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">Erik Hort</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">birthplace</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">county</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div> <p>This entity “country” might be matched pretty randomly. Or just one of the entity, say “Erik Hort” got matched. In either case, we end up with very limited starting state in the graph (later for PPR), or very misleading state.</p> <p>There is another way of doing this step 1:</p> <p><strong>Step 1’.</strong> In the repo of HippoRAG, they actually did not run directly NER on query (step 1), but</p> <p>retrieve the facts (relations). For example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query</span><span class="p">:</span> <span class="sh">"</span><span class="s">What county is Erik Hort</span><span class="sh">'</span><span class="s">s birthplace in?</span><span class="sh">"</span>
</code></pre></div></div> <p>Then we rerank facts using the same encoder model</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Top</span> <span class="n">facts</span><span class="p">:</span> <span class="p">[(</span><span class="sh">"</span><span class="s">Erik Hort</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">birthplace is</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Montebello</span><span class="sh">"</span><span class="p">),</span>
            <span class="p">(</span><span class="sh">"</span><span class="s">Montebello</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">is a part of</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Rockland County</span><span class="sh">"</span><span class="p">)]</span>
</code></pre></div></div> <p>Then use the entities from these selected fact relations as seeds in PPR later.</p> <p>Either step 1 and 1’ is trying to get some init seed in the PPR alg. Especially the ones from Step 1’ is called SEED nodes.</p> <table> <tbody> <tr> <td><strong>Step 2.</strong> After get the query nodes $R_q$ (seed nodes), PPR is excuted over the KG ( $</td> <td>N</td> <td>$ nodes and $</td> <td>E</td> <td>+</td> <td>E’</td> <td>$ edges (fact + synonymy).</td> </tr> </tbody> </table> <p>In this page we review the original pagerank alg.</p> <p>Here the PPR (personalized PageRank)</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>At each step:
<span class="p">
-</span> With probability α (damping): Follow a random outgoing edge
<span class="p">-</span> With probability (1-α): Jump back to SEED nodes (not uniform!)
</code></pre></div></div> <p>Stationary distribution is then given as</p> \[\pi = \alpha \pi M + (1-\alpha) s\] <p>where:</p> <ul> <li>M = transition matrix (based on graph structure)</li> <li>s = personalized reset distribution (seed nodes)</li> </ul> <p>The calculations of M and s are discussed below.</p> <p><strong>Calculation of M</strong></p> <p>So in this step, it is essentially about how we design the transition matrix.</p> <p>M is defined based on weighted edges.</p> <p>The weight of edges has 3 sources.</p> <ol> <li>Fact edges: Weight = Number of times this entity pair appears in triples</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">node_to_node_stats</span><span class="p">[(</span><span class="n">node_key</span><span class="p">,</span> <span class="n">node_2_key</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
<span class="n">self</span><span class="p">.</span><span class="n">node_to_node_stats</span><span class="p">[(</span><span class="n">node_2_key</span><span class="p">,</span> <span class="n">node_key</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>

<span class="c1"># ("CEO", "company") appears in 5 triples → weight = 5
</span></code></pre></div></div> <ol> <li>Passage Edges: weight = 1.0</li> <li>Synonymy Edges: weight = simiarity score ( between 0 1).</li> </ol> <p>Then given edge weights, the transition prob from node i to node j is given as</p> <p>$P(i → j) = \frac{weight(i,j)}{ \sum_k weight(i,k)}$</p> <p><strong>Calculation of s (reset distribution, reset prob, whatever)</strong></p> <ol> <li>It is not uniform.</li> <li>It is weighted by: Entity nodes and Passage nodes. For example</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Query</span> <span class="o">=</span> <span class="sh">"</span><span class="s">What county is Erik Hort</span><span class="sh">'</span><span class="s">s birthplace in?</span><span class="sh">"</span>

<span class="c1">#Top-k Facts (relations, step 1')
</span><span class="n">top_k_facts</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Erik Hort</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">birthplace is</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Montebello</span><span class="sh">"</span><span class="p">),</span>      <span class="c1"># score: 0.9
</span>    <span class="p">(</span><span class="sh">"</span><span class="s">Montebello</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">is a part of</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Rockland County</span><span class="sh">"</span><span class="p">),</span> <span class="c1"># score: 0.8
</span>    <span class="p">(</span><span class="sh">"</span><span class="s">Oliver Badman</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">is a</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">politician</span><span class="sh">"</span><span class="p">)</span>            <span class="c1"># score: 0.3
</span><span class="p">]</span>

<span class="c1"># Entity weights from facts
</span><span class="n">phrase_weights</span><span class="p">[</span><span class="nf">entity_id</span><span class="p">(</span><span class="sh">"</span><span class="s">Erik Hort</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.9</span>
<span class="n">phrase_weights</span><span class="p">[</span><span class="nf">entity_id</span><span class="p">(</span><span class="sh">"</span><span class="s">Montebello</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="p">(</span><span class="mf">0.9</span> <span class="o">+</span> <span class="mf">0.8</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span> <span class="o">=</span> <span class="mf">0.85</span>  <span class="c1"># Appears in 2 facts
</span><span class="n">phrase_weights</span><span class="p">[</span><span class="nf">entity_id</span><span class="p">(</span><span class="sh">"</span><span class="s">Rockland County</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.8</span>
<span class="n">phrase_weights</span><span class="p">[</span><span class="nf">entity_id</span><span class="p">(</span><span class="sh">"</span><span class="s">Oliver Badman</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.3</span>
<span class="n">phrase_weights</span><span class="p">[</span><span class="nf">entity_id</span><span class="p">(</span><span class="sh">"</span><span class="s">politician</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.3</span>

<span class="c1"># Passage weights (small, from dense retrieval)
</span><span class="n">passage_weights</span><span class="p">[</span><span class="nf">passage_id</span><span class="p">(</span><span class="sh">"</span><span class="s">P1</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.02</span>
<span class="n">passage_weights</span><span class="p">[</span><span class="nf">passage_id</span><span class="p">(</span><span class="sh">"</span><span class="s">P2</span><span class="sh">"</span><span class="p">)]</span> <span class="o">=</span> <span class="mf">0.01</span>

<span class="c1"># Reset distribution
</span><span class="n">reset_prob</span> <span class="o">=</span> <span class="n">phrase_weights</span> <span class="o">+</span> <span class="n">passage_weights</span>
<span class="c1"># Normalize: reset_prob = reset_prob / sum(reset_prob)
</span></code></pre></div></div> <p>Here the $s$ is not the one from paper section ‘Node Specificity’ which is just a parameter in front of initial distribution $\bar{n}$ (defined below).</p> <p>Then execution of PPR is just run the iteration until it converges. The initial distribution can be uniform among all query named entities, and other nodes with 0 probability.</p> <p>Denote the init distribution as $\bar{n}$.</p> <p>During PPR, the probability mass is distributed to nodes that are primarily in the (joint) neighborhood of the query nodes.</p> <p>Let us denote the converged probability distribution over $N$ as $\bar{n’}$, <strong>to obtain passage score, we need to</strong></p> \[\bar{p} = \bar{n'} \mathbf{P}\] <table> <tbody> <tr> <td>$\mathbf{P}$ is from the offline indexing, and this is a row vector multiply with a matrix, gives a row vec in $\mathbb{R}^{</td> <td>P</td> <td>}$.</td> </tr> </tbody> </table> <p>Then the top passages are returned to LLM.</p> <h1 id="conclusion">Conclusion</h1> <p>This paper provides a systematic way to find the most relevant entities to the query, and then the most relevant document chunks for LLM. I think this could be a potentially good method for structured data/table retrieval. More precisely, when we need to return several tables for a SQL generation, this method could give us an optimal path for table joins, namely 1-hop or 3-hop neighbors.</p> <h1 id="some-background-info">Some background info</h1> <h1 id="pagerank">PageRank</h1> <p>This the fundamental alg for Google to rank millions webpages. This is based on an assumption that user can start with random webpage and with some probability to perform a random walk. The becomes a Markov Chain.</p> <h1 id="markov-chain">markov chain</h1> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_14.56.17.png" width="60%" alt="Screenshot 2025-11-18 at 14.56.17.png"/></p> <p>Then the distribution of being each node is governed by</p> \[\pi_{n+1} = \pi_n P\] <p>where $P$ is the transition probability matrix.</p> <h2 id="stationary-distribution">Stationary distribution:</h2> \[\pi = \pi P\] <p>Theorem: For irreducible and aperiodic Markov chains:</p> <ol> <li>A unique stationary distribution $\pi$ exists</li> <li>All initial distributions $\pi_0$ converge to $\pi$</li> </ol> <p>Here irreducible basically means the graph is connected, and aperiodic mean the graph is not oscillating between two nodes.</p> <h3 id="calculation-of-stationary-distribution">Calculation of stationary distribution</h3> <ol> <li>Brutal</li> </ol> <p>Just start with any initial distribution, times multiple $P,$ i.e., $\pi_0 P^n$ until the residual smaller than a epsilon. Most practical</p> <ol> <li>Using linear equation solver</li> <li>Egenvector</li> </ol> \[\pi^T = P^T \pi^T\] <p>So $\pi^T$ is the eigenvector of $P^T$ of egenvalue 1.</p> <h1 id="pagerank-1">PageRank</h1> <p>Essentially calculating stationary distribution of a Markov chain.</p> <p>But with some variation:</p> <ol> <li> <p>Webpage with NO outgoing links. All eventually stop at that node</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_15.10.27.png" width="60%" alt="Screenshot 2025-11-18 at 15.10.27.png"/></p> </li> <li> <p>Cycles: Also end up with state 1 and 2, oscillating forever.</p> </li> </ol> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_15.11.18.png" width="60%" alt="Screenshot 2025-11-18 at 15.11.18.png"/></p> <p>PageRank is handling with Markov Chains that are NOT irreducible and aperiodic!</p> <p>Idea: if the user is stuck in a state, randomly select a new state</p> <p>Step 1. Given a parameter $\alpha\in(0,1)$, with probability $\frac{\alpha}{N}$ where N is the number of states (nodes), we can transition to a random state.</p> <p>For example</p> <p><img src="/assets/img/hipporag/Screenshot_2025-11-18_at_15.17.15.png" width="60%" alt="Screenshot 2025-11-18 at 15.17.15.png"/></p> <p>But now, the probability sum is NOT 1 anymore.</p> <p>Step 2. Adding a damping factor: reduce original transition probabilities by factor of $(1-\alpha)$ and then add the random state transition $\frac{\alpha}{N}$.</p> <p>Mathematically, we modify the matrix $P$ to $\bar{P}$</p> \[\bar{P} = (1-\alpha) P + \frac{\alpha}{N} \mathbf{1}_{N\times N}\] <p>that is N by N all 1 matrix.</p> <p>PageRank is then just finding the stationary distribution of $\bar{P}$.</p>]]></content><author><name></name></author><category term="paper-reading"/><category term="paper-notes"/><category term="rag"/><category term="llm"/><category term="knowledge-graph"/><category term="pagerank"/><summary type="html"><![CDATA[Paper reading notes on HippoRAG - using knowledge graphs and PageRank for better RAG retrieval]]></summary></entry></feed>